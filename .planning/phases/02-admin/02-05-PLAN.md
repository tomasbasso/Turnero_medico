---
phase: 02-admin
plan: 05
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - app/(admin)/especialidades/page.tsx
  - components/admin/SpecialtiesList.tsx
  - components/admin/SpecialtyForm.tsx
autonomous: true
requirements: [SC-2, SC-5]

must_haves:
  truths:
    - "Specialties list page is a Server Component that reads x-user-role header and fetches active specialties via Prisma"
    - "ADMIN sees 'Crear especialidad' button; RECEPTIONIST does not (excluded from JSX, not display:none)"
    - "Each specialty row shows color swatch (20x20 circle), name, description, and Editar+Eliminar buttons (ADMIN only)"
    - "SpecialtyForm inside the Drawer creates or edits a specialty via fetch to POST/PUT /api/admin/specialties"
    - "On successful save, drawer closes and router.refresh() re-runs the Server Component to show updated list"
    - "Inline delete confirmation (not modal) shows per-row; on confirm, DELETE /api/admin/specialties/[id] called then router.refresh()"
    - "Empty state shows when no specialties exist"
    - "Loading spinner on submit button; error message below button on failure"
  artifacts:
    - path: "app/(admin)/especialidades/page.tsx"
      provides: "Specialties list Server Component page"
      contains: "prisma.specialty.findMany"
    - path: "components/admin/SpecialtiesList.tsx"
      provides: "Client Component managing drawer state + list rendering + inline delete confirm"
      exports: ["SpecialtiesList"]
    - path: "components/admin/SpecialtyForm.tsx"
      provides: "Client Component form for create/edit specialty with color picker"
      exports: ["SpecialtyForm"]
  key_links:
    - from: "app/(admin)/especialidades/page.tsx"
      to: "components/admin/SpecialtiesList.tsx"
      via: "SpecialtiesList import"
      pattern: "SpecialtiesList"
    - from: "components/admin/SpecialtiesList.tsx"
      to: "components/admin/Drawer.tsx"
      via: "Drawer import"
      pattern: "from '@/components/admin/Drawer'"
    - from: "components/admin/SpecialtiesList.tsx"
      to: "components/admin/SpecialtyForm.tsx"
      via: "SpecialtyForm import"
      pattern: "SpecialtyForm"
    - from: "components/admin/SpecialtyForm.tsx"
      to: "app/api/admin/specialties/route.ts"
      via: "fetch to /api/admin/specialties"
      pattern: "/api/admin/specialties"
---

<objective>
Specialties ABM UI: Server Component list page + Client Component drawer with create/edit form + inline delete confirmation.

Purpose: Satisfies SC-2 (Admin can create, edit and delete specialties with color). Follows the list + drawer pattern locked in CONTEXT.md. Role-gated: ADMIN sees CRUD controls, RECEPTIONIST sees read-only list.

Output: app/(admin)/especialidades/page.tsx, components/admin/SpecialtiesList.tsx, components/admin/SpecialtyForm.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-admin/02-CONTEXT.md
@.planning/phases/02-admin/02-RESEARCH.md
@.planning/phases/02-admin/02-PATTERNS.md
@.planning/phases/02-admin/02-UI-SPEC.md
@.planning/phases/02-admin/02-01-SUMMARY.md
@.planning/phases/02-admin/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create specialties page and SpecialtiesList component</name>
  <files>app/(admin)/especialidades/page.tsx, components/admin/SpecialtiesList.tsx</files>

  <read_first>
    - app/(admin)/admin/page.tsx — read to understand existing admin page structure (p-6 padding, font-display heading pattern)
    - components/admin/AdminSidebar.tsx — read to confirm cn() usage pattern, existing nav items
    - components/admin/Drawer.tsx — read to confirm Drawer props: open, onClose, title, children (from Plan 01)
    - lib/utils.ts — confirm cn() signature
    - .planning/phases/02-admin/02-PATTERNS.md — especialidades/page.tsx section, SpecialtiesList.tsx section (drawer state, delete pattern, role-gated buttons)
    - .planning/phases/02-admin/02-UI-SPEC.md — Specialties List screen (row anatomy, empty state copy, role-gated elements table), Delete Specialty Confirmation screen
    - app/globals.css — confirm text-error token
  </read_first>

  <action>
    FILE A: app/(admin)/especialidades/page.tsx — async Server Component, no 'use client'.

    Imports: headers from 'next/headers'; prisma from '@/lib/prisma'; SpecialtiesList from '@/components/admin/SpecialtiesList'.

    Function body:
    - const headersList = await headers() — MUST await (Next.js 15+/16 breaking change)
    - const role = headersList.get('x-user-role') as 'ADMIN' | 'RECEPTIONIST'
    - const specialties = await prisma.specialty.findMany({ where: { isActive: true }, orderBy: { name: 'asc' } })
    - Return JSX: div className="p-6" containing h1 "Especialidades" (font-display text-xl font-semibold text-text-primary) and SpecialtiesList with props { specialties, role }

    ---

    FILE B: components/admin/SpecialtiesList.tsx — Client Component ('use client').

    Imports: useState from 'react'; useRouter from 'next/navigation'; Edit and Trash2 from 'lucide-react'; cn from '@/lib/utils'; Drawer from '@/components/admin/Drawer'; SpecialtyForm from '@/components/admin/SpecialtyForm'.

    Define Specialty type inline: { id: number; name: string; description: string | null; color: string; isActive: boolean }

    Props: specialties: Specialty[], role: 'ADMIN' | 'RECEPTIONIST'

    State:
    - const [drawerOpen, setDrawerOpen] = useState(false)
    - const [editing, setEditing] = useState<Specialty | null>(null)
    - const [deletingId, setDeletingId] = useState<number | null>(null)

    Handlers:
    - openCreate: setEditing(null); setDrawerOpen(true)
    - openEdit(s): setEditing(s); setDrawerOpen(true)
    - closeDrawer: setDrawerOpen(false); setEditing(null)
    - handleDelete(id): async — call fetch('/api/admin/specialties/' + id, { method: 'DELETE' }) then router.refresh() then setDeletingId(null)

    JSX structure:
    - Outer div containing:
      1. Header row: div className="flex items-center justify-between mb-6" — left: h1 (or use page's h1; the list just renders its content without duplicate heading), right: if role === 'ADMIN', a button "Crear especialidad" (primary style) with onClick={openCreate}. Note: the page already renders the h1, so this header row is within the list component and only shows the button.
      2. If specialties.length === 0: empty state div centered — heading "Sin especialidades" (text-text-primary font-semibold), body "Aún no hay especialidades registradas. Creá la primera para empezar." (text-text-secondary text-sm), and if role === 'ADMIN' a "Crear especialidad" button
      3. If specialties.length > 0: div className="bg-surface rounded-xl shadow-card" with a list of rows
    
    Each row: div className="flex items-center gap-4 px-6 py-4 border-b border-border last:border-b-0"
    - Color swatch: div with style={{ backgroundColor: specialty.color }} and className="h-5 w-5 rounded-full flex-shrink-0"
    - Name: span className="text-sm font-semibold text-text-primary flex-1" showing specialty.name
    - Description: span className="text-sm text-text-secondary flex-1 truncate" showing specialty.description or empty
    - Actions (only if role === 'ADMIN'): conditional on deletingId === specialty.id showing inline confirm, otherwise showing icon buttons

    Idle action buttons (when deletingId !== specialty.id):
    - Edit button: button with aria-label="Editar especialidad", onClick={() => openEdit(specialty)}, className="text-text-secondary hover:text-text-primary transition-colors p-1", showing Edit icon h-4 w-4
    - Delete button: button with aria-label="Eliminar especialidad", onClick={() => setDeletingId(specialty.id)}, className="text-error hover:text-error transition-colors p-1", showing Trash2 icon h-4 w-4

    Inline confirm state (when deletingId === specialty.id):
    - div className="flex items-center gap-2 transition-opacity opacity-100 duration-150"
    - span className="text-xs text-text-secondary" showing "¿Confirmar eliminación?"
    - button className="text-sm font-semibold text-error" onClick={() => handleDelete(specialty.id)} showing "Sí, eliminar"
    - button className="text-sm text-text-secondary" onClick={() => setDeletingId(null)} showing "Descartar cambios"

    Drawer at bottom of JSX: Drawer component with open={drawerOpen}, onClose={closeDrawer}, title={editing ? 'Editar especialidad' : 'Crear especialidad'}. Inside Drawer: SpecialtyForm with props { specialty: editing, onClose: closeDrawer }.

    Typography: only font-semibold (600) and regular (400). No font-medium or font-bold anywhere.
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - app/(admin)/especialidades/page.tsx does NOT contain 'use client'
    - page.tsx contains `await headers()` (not headers() without await)
    - page.tsx contains `prisma.specialty.findMany`
    - components/admin/SpecialtiesList.tsx starts with 'use client'
    - SpecialtiesList renders the Drawer component with open, onClose, title props
    - SpecialtiesList action buttons have aria-label="Editar especialidad" and aria-label="Eliminar especialidad"
    - Role guard: buttons only rendered when `role === 'ADMIN'` (JSX conditional, not CSS display:none)
    - Inline delete confirm shows "¿Confirmar eliminación?" copy (matching UI-SPEC exactly)
    - Confirm delete button shows "Sí, eliminar"
    - Dismiss button shows "Descartar cambios"
    - Empty state shows "Sin especialidades" heading
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>Specialties page and list component working. Role-gated CRUD controls. Inline delete confirm. Drawer integration ready for SpecialtyForm.</done>
</task>

<task type="auto">
  <name>Task 2: Create SpecialtyForm component</name>
  <files>components/admin/SpecialtyForm.tsx</files>

  <read_first>
    - app/(auth)/login/page.tsx — read in full; establishes the exact fetch + useState + router.refresh pattern, input style, button style, error display pattern that SpecialtyForm must replicate
    - .planning/phases/02-admin/02-PATTERNS.md — SpecialtyForm.tsx section (controlled inputs, submit handler with two-step fetch, color picker pattern)
    - .planning/phases/02-admin/02-UI-SPEC.md — Specialty Create/Edit Form section (field specs, input style, label style, footer with "Guardar especialidad" + "Descartar cambios", error copy)
    - .planning/phases/02-admin/02-RESEARCH.md — Pattern 6 (mutation with router.refresh), Code Examples (input style, button style, color picker)
  </read_first>

  <action>
    Create components/admin/SpecialtyForm.tsx as a Client Component.

    'use client' at top.

    Imports: useState from 'react'; useRouter from 'next/navigation'; Loader2 from 'lucide-react'.

    Define Specialty type: { id: number; name: string; description: string | null; color: string }

    Props: specialty (Specialty | null), onClose (() => void)

    Controlled state initialized from specialty prop (or defaults):
    - const [name, setName] = useState(specialty?.name ?? '')
    - const [description, setDescription] = useState(specialty?.description ?? '')
    - const [color, setColor] = useState(specialty?.color ?? '#0d9488')
    - const [loading, setLoading] = useState(false)
    - const [error, setError] = useState('')

    handleSubmit:
    - e.preventDefault()
    - Client validation: if (!name.trim()) setError('El nombre es requerido'); return
    - setLoading(true); setError('')
    - fetch URL: specialty ? '/api/admin/specialties/' + specialty.id : '/api/admin/specialties'
    - method: specialty ? 'PUT' : 'POST'
    - body: JSON.stringify({ name: name.trim(), description: description || null, color })
    - On !res.ok: parse error message and setError(data.error ?? 'Ocurrió un error al guardar. Intentá nuevamente.')
    - On success: onClose(); router.refresh()
    - catch: setError('Ocurrió un error al guardar. Intentá nuevamente.')
    - finally: setLoading(false)

    JSX: form with onSubmit={handleSubmit}, className="flex flex-col gap-4"

    Fields:
    1. Nombre (required):
       - label className="block text-xs text-text-secondary mb-1" text "Nombre"
       - input type="text", value={name}, onChange update setName, maxLength={100}, required, className="w-full rounded-lg border border-border bg-white px-3 py-2 text-sm text-text-primary focus:outline-none focus:border-[rgba(13,148,136,0.4)] focus:ring-2 focus:ring-[rgba(13,148,136,0.2)] transition-all"

    2. Descripción (optional):
       - label text "Descripción"
       - textarea rows={3}, value={description}, onChange update setDescription, maxLength={500}, className same pattern as input

    3. Color:
       - label text "Color"
       - div className="flex items-center gap-3" containing:
         a. input type="color", value={color}, onChange update setColor, className="h-8 w-8 rounded cursor-pointer border-0 p-0 bg-transparent"
         b. div className="h-6 w-6 rounded-full" with style={{ backgroundColor: color }} as swatch preview

    Error display (if error): p className="rounded-lg bg-red-50 px-4 py-2.5 text-sm text-error" showing {error}

    Footer: div className="flex gap-3 pt-2"
    - "Descartar cambios" button: type="button" (not submit), onClick={onClose}, className="flex-1 rounded-lg border border-[rgba(13,148,136,0.3)] px-4 py-2 text-sm font-semibold text-primary transition-all duration-150 hover:bg-primary/5"
    - "Guardar especialidad" button: type="submit", disabled={loading}, className="flex-1 rounded-lg bg-gradient-to-r from-primary to-accent px-4 py-2 text-sm font-semibold text-white transition-all duration-150 hover:shadow-[0_0_12px_rgba(20,184,166,0.4)] disabled:opacity-50 disabled:cursor-not-allowed disabled:pointer-events-none". When loading: show Loader2 h-4 w-4 animate-spin mx-auto; when not loading: text "Guardar especialidad".

    Typography: only font-semibold (600) and regular (400). No font-medium or font-bold.
    CTA label exact copy per UI-SPEC: "Guardar especialidad". Dismiss exact copy: "Descartar cambios".
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - components/admin/SpecialtyForm.tsx starts with 'use client'
    - Form submit handler calls fetch to '/api/admin/specialties' (POST) or '/api/admin/specialties/${specialty.id}' (PUT)
    - On success: onClose() called then router.refresh() (in this order)
    - Submit button text is exactly "Guardar especialidad" (per UI-SPEC)
    - Dismiss button text is exactly "Descartar cambios" (per UI-SPEC)
    - Dismiss button has type="button" (not type="submit")
    - Color field uses input type="color" with a swatch preview div showing current color via style={{ backgroundColor: color }}
    - Error message copy contains "Ocurrió un error al guardar. Intentá nuevamente."
    - Loading state: Loader2 with animate-spin replaces button text
    - Submit button has disabled={loading} with disabled styles
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>SpecialtyForm creates and edits specialties, shows loading/error feedback, closes drawer and refreshes list on success. SC-5 (visual feedback) satisfied for specialties module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Client Component → API Route Handler | SpecialtyForm sends fetch; auth enforced server-side by requireAdmin |
| Server Component → Middleware headers | page.tsx reads x-user-role from middleware-injected headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Elevation of Privilege | SpecialtiesList — role gate bypass via DOM manipulation | mitigate | Buttons excluded from JSX for RECEPTIONIST (not display:none); API returns 403 regardless of client state |
| T-02-05-02 | Information Disclosure | x-user-role header read in Server Component | accept | Header injected by middleware only on authenticated requests; cannot be spoofed by client for Server Component reads |
| T-02-05-03 | Tampering | SpecialtyForm — CSRF via cross-origin fetch | accept | SameSite=lax cookie provides adequate CSRF protection per ASVS L1; no custom CSRF token required at this level |
| T-02-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan |
</threat_model>

<verification>
After both tasks:
- npx tsc --noEmit passes
- grep -c 'use client' app/(admin)/especialidades/page.tsx returns 0
- grep -c "'use client'" components/admin/SpecialtiesList.tsx returns 1
- grep -c 'aria-label="Editar especialidad"' components/admin/SpecialtiesList.tsx returns 1
- grep -c 'aria-label="Eliminar especialidad"' components/admin/SpecialtiesList.tsx returns 1
- grep -c 'Guardar especialidad' components/admin/SpecialtyForm.tsx returns at least 1
- grep -c 'Descartar cambios' components/admin/SpecialtyForm.tsx returns at least 1
- grep -c 'router.refresh' components/admin/SpecialtyForm.tsx returns 1
</verification>

<success_criteria>
- Navigating to /admin/especialidades shows specialty list (or empty state)
- ADMIN: sees "Crear especialidad" button; clicking opens Drawer with SpecialtyForm
- Create specialty: fill name/description/color picker, click "Guardar especialidad" → list updates
- Edit specialty: click edit icon → drawer opens pre-filled → save → list updates
- Delete specialty: click trash → inline confirm row appears → "Sí, eliminar" → list updates
- RECEPTIONIST: sees list, no CRUD buttons visible
- Loading spinner during save; error message on failure
</success_criteria>

<output>
Create `.planning/phases/02-admin/02-05-SUMMARY.md` when done
</output>
