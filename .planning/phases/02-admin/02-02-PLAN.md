---
phase: 02-admin
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/(admin)/admin/page.tsx
  - components/admin/StatCard.tsx
autonomous: true
requirements: [SC-1]

must_haves:
  truths:
    - "Dashboard page is an async Server Component that queries prisma.appointment.groupBy by status for the current week"
    - "4 StatCards render: Pendientes (amber), Confirmados (teal), Completados (slate), Cancelados (red)"
    - "When no appointments exist for the week, all cards show 0 (no crash, no empty state)"
    - "Dashboard subtitle shows 'Semana del {dd de mes} al {dd de mes, yyyy}' derived from getWeekRange() dates"
    - "No client-side fetch, no loading skeleton (per D-04)"
  artifacts:
    - path: "app/(admin)/admin/page.tsx"
      provides: "Real dashboard Server Component"
      contains: "prisma.appointment.groupBy"
    - path: "components/admin/StatCard.tsx"
      provides: "Stat card display component"
      exports: ["StatCard"]
  key_links:
    - from: "app/(admin)/admin/page.tsx"
      to: "lib/utils.ts"
      via: "getWeekRange import"
      pattern: "getWeekRange"
    - from: "app/(admin)/admin/page.tsx"
      to: "components/admin/StatCard.tsx"
      via: "StatCard import"
      pattern: "StatCard"
    - from: "components/admin/StatCard.tsx"
      to: "lib/utils.ts"
      via: "STATUS_COLORS and STATUS_LABELS imports"
      pattern: "STATUS_COLORS"
---

<objective>
Replace the dashboard placeholder with a real Server Component that queries current-week appointment counts grouped by status, and renders 4 StatCards per the UI-SPEC.

Purpose: Satisfies SC-1 (dashboard shows real weekly metrics) and D-01 through D-04 (current week, 4 cards, no graphs, Server Component + direct Prisma).

Output: app/(admin)/admin/page.tsx (replaced), components/admin/StatCard.tsx (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-admin/02-CONTEXT.md
@.planning/phases/02-admin/02-RESEARCH.md
@.planning/phases/02-admin/02-PATTERNS.md
@.planning/phases/02-admin/02-UI-SPEC.md
@.planning/phases/02-admin/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatCard component</name>
  <files>components/admin/StatCard.tsx</files>

  <read_first>
    - lib/utils.ts — read to confirm STATUS_COLORS and STATUS_LABELS exact keys and values (PENDING/CONFIRMED/CANCELLED/COMPLETED)
    - app/globals.css — read to confirm shadow-card, bg-surface, text-text-primary, text-text-secondary token names
    - .planning/phases/02-admin/02-UI-SPEC.md — Dashboard screen section (stat card anatomy: bg-surface rounded-xl shadow-card p-6 border-l-4 {color})
    - .planning/phases/02-admin/02-PATTERNS.md — StatCard.tsx section (STATUS_BORDER constants, card structure)
  </read_first>

  <action>
    Create components/admin/StatCard.tsx. This is a Server Component (no 'use client').

    Imports: cn from '@/lib/utils'; STATUS_LABELS and STATUS_COLORS from '@/lib/utils'. Import lucide icons: Clock (for PENDING), CheckCircle2 (for CONFIRMED), CheckCheck (for COMPLETED), XCircle (for CANCELLED).

    Define STATUS_BORDER as a const Record mapping:
    - PENDING → 'border-amber-400'
    - CONFIRMED → 'border-teal-500'
    - COMPLETED → 'border-slate-400'
    - CANCELLED → 'border-red-400'

    Define STATUS_ICONS mapping the four statuses to their lucide icon components.

    Props interface StatCardProps: status (string), count (number).

    The card JSX:
    - Outer div: className={cn('bg-surface rounded-xl shadow-card p-6 border-l-4', STATUS_BORDER[status])}
    - Top row: div with className="flex items-center justify-between mb-2". Left: span with className={cn('text-xs', STATUS_COLORS[status])} showing STATUS_LABELS[status]. Right: the status icon, h-4 w-4, also colored with STATUS_COLORS[status].
    - Count: p with className="font-display text-3xl font-semibold text-text-primary" showing {count}.
    - Sub-label: p with className="text-xs text-text-secondary mt-1" showing "turnos esta semana".

    Export StatCard as a named export.

    Typography rule: only font-semibold (600) and regular (400). No font-medium or font-bold anywhere in this file.
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - components/admin/StatCard.tsx exports `StatCard` as a named export
    - File does NOT contain 'use client'
    - STATUS_BORDER record has all four keys: PENDING, CONFIRMED, COMPLETED, CANCELLED
    - The card outer div uses 'border-l-4' with the STATUS_BORDER dynamic class via cn()
    - Count paragraph uses 'font-display text-3xl font-semibold text-text-primary'
    - Sub-label uses 'text-xs text-text-secondary'
    - No 'font-medium' or 'font-bold' class anywhere in the file
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>StatCard renders 4 visual states (amber/teal/slate/red borders), shows count and "turnos esta semana", TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard placeholder with real Server Component</name>
  <files>app/(admin)/admin/page.tsx</files>

  <read_first>
    - app/(admin)/admin/page.tsx — read current placeholder (will be fully replaced)
    - lib/prisma.ts — confirm prisma singleton import path is '@/lib/prisma'
    - lib/utils.ts — confirm getWeekRange is now exported (from 02-01 plan)
    - .planning/phases/02-admin/02-RESEARCH.md — Pattern 5 (groupBy query, stats transform, getWeekRange usage)
    - .planning/phases/02-admin/02-PATTERNS.md — app/(admin)/admin/page.tsx section
    - .planning/phases/02-admin/02-UI-SPEC.md — Dashboard screen section (layout p-6, subtitle copy, grid grid-cols-2 gap-4 md:grid-cols-4)
    - .planning/phases/02-admin/02-CONTEXT.md — D-01 (current week), D-02 (4 cards), D-03 (cards only), D-04 (Server Component)
  </read_first>

  <action>
    Replace app/(admin)/admin/page.tsx entirely. This is an async Server Component (no 'use client').

    Imports: prisma from '@/lib/prisma'; StatCard from '@/components/admin/StatCard'; getWeekRange and formatDate from '@/lib/utils'.

    The page function is: export default async function AdminDashboardPage()

    Steps inside the function:
    1. const { monday, sunday } = getWeekRange()
    2. Query: const counts = await prisma.appointment.groupBy({ by: ['status'], where: { date: { gte: monday, lte: sunday } }, _count: { _all: true } })
    3. Transform to stats object: const stats = { PENDING: 0, CONFIRMED: 0, COMPLETED: 0, CANCELLED: 0 } then for (const row of counts) stats[row.status as keyof typeof stats] = row._count._all
    4. Build subtitle string using formatDate(monday) and formatDate(sunday) — the subtitle must read exactly "Semana del {formatted monday} al {formatted sunday}" (per UI-SPEC copywriting contract). Note: formatDate in lib/utils.ts uses 'es-AR' locale with day/month/year.

    JSX structure:
    - Outer div: className="p-6"
    - Header section: div className="mb-8". Inside: h1 className="font-display text-xl font-semibold text-text-primary" with text "Dashboard". Below: p className="text-sm text-text-secondary mt-1" showing the subtitle string.
    - Cards grid: div className="grid grid-cols-2 gap-4 md:grid-cols-4". Four StatCard components in this order: PENDING (stats.PENDING), CONFIRMED (stats.CONFIRMED), COMPLETED (stats.COMPLETED), CANCELLED (stats.CANCELLED).

    The order of cards MUST be: Pendientes, Confirmados, Completados, Cancelados (per UI-SPEC).

    No loading state, no skeleton, no Suspense wrapper on the grid (per D-04).
    No import from 'next/headers' — dashboard does not need role check (all authenticated users see the same dashboard).
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - app/(admin)/admin/page.tsx does NOT contain 'use client'
    - File contains 'prisma.appointment.groupBy'
    - File contains 'getWeekRange'
    - File renders exactly 4 StatCard components with status props: 'PENDING', 'CONFIRMED', 'COMPLETED', 'CANCELLED'
    - File contains 'grid grid-cols-2 gap-4 md:grid-cols-4'
    - File does NOT contain 'Skeleton', 'loading', 'Suspense', or 'fetch('
    - Subtitle contains 'Semana del' string literal
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>Dashboard page is a real Server Component querying current-week appointments, rendering 4 StatCards. SC-1 and D-01 through D-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → PostgreSQL | Prisma groupBy query with date range filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Dashboard RSC — appointment count leakage | accept | Data is aggregate counts only (no patient PII); visible only to authenticated users (middleware guards the /admin route) |
| T-02-02-02 | Tampering | Prisma groupBy — date boundary input | accept | getWeekRange() runs server-side; no user-controlled input to this query; date values are computed from server clock |
| T-02-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan |
</threat_model>

<verification>
After both tasks:
- npx tsc --noEmit passes (0 errors)
- grep -c 'prisma.appointment.groupBy' app/(admin)/admin/page.tsx returns 1
- grep -c 'StatCard' app/(admin)/admin/page.tsx returns at least 4 (4 usages + 1 import)
- grep -c 'use client' app/(admin)/admin/page.tsx returns 0
- grep -c 'font-medium\|font-bold' components/admin/StatCard.tsx returns 0
</verification>

<success_criteria>
- Dashboard shows 4 stat cards grouped by appointment status for current week
- Cards use correct border colors: amber (PENDING), teal (CONFIRMED), slate (COMPLETED), red (CANCELLED)
- Subtitle shows current week date range in Spanish (es-AR locale)
- No client-side fetch, no skeleton, no loading state on dashboard
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/02-admin/02-02-SUMMARY.md` when done
</output>
