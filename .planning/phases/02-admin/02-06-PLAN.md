---
phase: 02-admin
plan: 06
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-04"
files_modified:
  - app/(admin)/medicos/page.tsx
  - components/admin/DoctorsList.tsx
  - components/admin/DoctorForm.tsx
autonomous: true
requirements: [SC-3, SC-5]

must_haves:
  truths:
    - "Doctors list page is a Server Component fetching doctors with specialty included"
    - "Each doctor row shows 40x40 avatar (image or initials fallback), name, specialty chip with color dot, phone, duration"
    - "ADMIN sees 'Crear médico' button, Editar and Eliminar controls; RECEPTIONIST sees list + Disponibilidad link only"
    - "DoctorForm inside Drawer handles create (two-step: POST then avatar upload then PATCH) and edit (PUT with all fields)"
    - "Avatar file input shows preview via URL.createObjectURL before upload; initials fallback shown when no file/avatar"
    - "Inline delete confirmation for each doctor row; on confirm, DELETE /api/admin/doctors/[id] then router.refresh()"
    - "Loading, error, and success feedback present during all async operations"
  artifacts:
    - path: "app/(admin)/medicos/page.tsx"
      provides: "Doctors list Server Component page"
      contains: "prisma.doctor.findMany"
    - path: "components/admin/DoctorsList.tsx"
      provides: "Client Component with drawer state, list rendering, inline delete confirm, Disponibilidad link"
      exports: ["DoctorsList"]
    - path: "components/admin/DoctorForm.tsx"
      provides: "Client Component form with avatar upload preview and two-step create flow"
      exports: ["DoctorForm"]
  key_links:
    - from: "app/(admin)/medicos/page.tsx"
      to: "components/admin/DoctorsList.tsx"
      via: "DoctorsList import"
      pattern: "DoctorsList"
    - from: "components/admin/DoctorsList.tsx"
      to: "components/admin/Drawer.tsx"
      via: "Drawer import"
      pattern: "from '@/components/admin/Drawer'"
    - from: "components/admin/DoctorForm.tsx"
      to: "lib/supabase.ts"
      via: "uploadAvatar import"
      pattern: "uploadAvatar"
    - from: "components/admin/DoctorForm.tsx"
      to: "app/api/admin/doctors/route.ts"
      via: "fetch to /api/admin/doctors"
      pattern: "/api/admin/doctors"
---

<objective>
Doctors ABM UI: Server Component list page + Client Component drawer with create/edit form including avatar upload with preview + inline delete confirmation + Disponibilidad link.

Purpose: Satisfies SC-3 (Admin can create, edit and delete doctors with avatar upload to Supabase Storage). Mirrors the Specialties ABM pattern with the additional two-step create flow for avatars.

Output: app/(admin)/medicos/page.tsx, components/admin/DoctorsList.tsx, components/admin/DoctorForm.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-admin/02-CONTEXT.md
@.planning/phases/02-admin/02-RESEARCH.md
@.planning/phases/02-admin/02-PATTERNS.md
@.planning/phases/02-admin/02-UI-SPEC.md
@.planning/phases/02-admin/02-01-SUMMARY.md
@.planning/phases/02-admin/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create doctors page and DoctorsList component</name>
  <files>app/(admin)/medicos/page.tsx, components/admin/DoctorsList.tsx</files>

  <read_first>
    - app/(admin)/especialidades/page.tsx — read to replicate the identical Server Component pattern (await headers, role, findMany, pass to Client Component)
    - components/admin/SpecialtiesList.tsx — read to understand the drawer state + inline delete confirm + role-gated buttons pattern from Plan 05
    - lib/utils.ts — confirm getInitials signature: getInitials(name: string): string
    - .planning/phases/02-admin/02-PATTERNS.md — medicos/page.tsx section (specialty include with color), DoctorsList.tsx section (avatar fallback, specialty chip, Disponibilidad link)
    - .planning/phases/02-admin/02-UI-SPEC.md — Doctors List screen (row anatomy, avatar 40x40, specialty chip, phone, duration "XX min", actions, Disponibilidad link), Delete Doctor Confirmation (copy differs: "¿Confirmar eliminación? Esta acción no se puede deshacer.")
    - .planning/phases/02-admin/02-CONTEXT.md — Permission rules for Disponibilidad link (visible to RECEPTIONIST as read-only)
  </read_first>

  <action>
    FILE A: app/(admin)/medicos/page.tsx — async Server Component, no 'use client'.

    Imports: headers from 'next/headers'; prisma from '@/lib/prisma'; DoctorsList from '@/components/admin/DoctorsList'.

    Function body:
    - const headersList = await headers() — MUST await
    - const role = headersList.get('x-user-role') as 'ADMIN' | 'RECEPTIONIST'
    - const [specialties, doctors] = await Promise.all([ prisma.specialty.findMany({ where: { isActive: true }, orderBy: { name: 'asc' }, select: { id: true, name: true, color: true } }), prisma.doctor.findMany({ where: { isActive: true }, orderBy: { name: 'asc' }, include: { specialty: { select: { id: true, name: true, color: true } } } }) ])
    - Return JSX: div className="p-6" containing h1 "Médicos" (font-display text-xl font-semibold text-text-primary) and DoctorsList with props { doctors, specialties, role }

    Note: specialties list is passed to DoctorsList so the DoctorForm select dropdown can populate without an extra API call.

    ---

    FILE B: components/admin/DoctorsList.tsx — Client Component ('use client').

    Imports: useState from 'react'; useRouter from 'next/navigation'; Link from 'next/link'; Edit, Trash2 from 'lucide-react'; cn and getInitials from '@/lib/utils'; Drawer from '@/components/admin/Drawer'; DoctorForm from '@/components/admin/DoctorForm'.

    Define Doctor type inline matching Prisma output: { id: number; name: string; avatar: string | null; bio: string | null; phone: string | null; durationMin: number; specialty: { id: number; name: string; color: string } }
    Define Specialty type: { id: number; name: string; color: string }

    Props: doctors: Doctor[], specialties: Specialty[], role: 'ADMIN' | 'RECEPTIONIST'

    State: drawerOpen, editing (Doctor | null), deletingId (number | null) — same pattern as SpecialtiesList.

    Handler: handleDelete(id) — fetch DELETE, router.refresh(), clear deletingId.

    JSX structure:
    - Header row (inside component): if role === 'ADMIN', show "Crear médico" primary button (left of h1, or right side consistent with Specialties)
    - Empty state: "Sin médicos" heading, "Aún no hay médicos registrados. Agregá el primero para comenzar." body, "Crear médico" CTA (ADMIN only)
    - List: div className="bg-surface rounded-xl shadow-card"

    Each doctor row: div className="flex items-center gap-4 px-6 py-4 border-b border-border last:border-b-0"
    
    1. Avatar (40x40): if doctor.avatar exists, img with src={doctor.avatar} alt={doctor.name} className="h-10 w-10 rounded-full object-cover flex-shrink-0". Else: div className="h-10 w-10 rounded-full bg-primary/20 flex items-center justify-center text-sm font-semibold text-primary flex-shrink-0" showing getInitials(doctor.name).
    
    2. Name + specialty chip: div className="flex-1 min-w-0". Inside: span className="text-sm font-semibold text-text-primary block" for name. Below: div className="flex items-center gap-1.5 mt-0.5" containing: div with className="h-2 w-2 rounded-full flex-shrink-0" and style={{ backgroundColor: doctor.specialty.color }} as color dot; span className="text-xs bg-teal-100 text-teal-700 rounded-full px-2 py-0.5" showing doctor.specialty.name.
    
    3. Phone: span className="text-sm text-text-secondary hidden sm:block" showing doctor.phone or "—"
    
    4. Duration: span className="text-xs text-text-muted" showing doctor.durationMin + " min"
    
    5. Actions (right-aligned flex gap-1): 
       - Disponibilidad link (visible to ALL roles): Link href={'/admin/medicos/' + doctor.id + '/disponibilidad'} className="text-xs font-semibold text-accent hover:text-primary transition-colors px-2 py-1" showing "Disponibilidad"
       - ADMIN-only buttons (if role === 'ADMIN' and deletingId !== doctor.id): Edit icon button (aria-label="Editar médico") + Trash2 icon button (aria-label="Eliminar médico", text-error)
       - Inline confirm (if role === 'ADMIN' and deletingId === doctor.id): "¿Confirmar eliminación? Esta acción no se puede deshacer." text + "Sí, eliminar" button (text-error font-semibold) + "Descartar cambios" button (text-text-secondary)

    Drawer at bottom: Drawer with title={editing ? 'Editar médico' : 'Crear médico'} containing DoctorForm with { doctor: editing, specialties, onClose: closeDrawer }.

    Typography: only font-semibold (600) and regular (400). No font-medium or font-bold.
    Aria-labels exact per UI-SPEC: "Editar médico", "Eliminar médico".
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - app/(admin)/medicos/page.tsx does NOT contain 'use client'
    - page.tsx contains `await headers()` and `prisma.doctor.findMany`
    - page.tsx fetches both doctors and specialties in Promise.all (one round-trip)
    - DoctorsList.tsx starts with 'use client'
    - DoctorsList renders avatar img (with object-cover) OR initials fallback via getInitials()
    - DoctorsList renders specialty chip with color dot (style={{ backgroundColor: doctor.specialty.color }})
    - Disponibilidad Link is rendered for ALL roles (including RECEPTIONIST)
    - Edit/Eliminar buttons have aria-label="Editar médico" and aria-label="Eliminar médico"
    - Delete confirm copy: "¿Confirmar eliminación? Esta acción no se puede deshacer." (differs from specialty confirm)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>Doctors page and list component working. Avatar display, specialty chip, role-gated CRUD, inline delete confirm, Disponibilidad link for all roles.</done>
</task>

<task type="auto">
  <name>Task 2: Create DoctorForm component with avatar upload</name>
  <files>components/admin/DoctorForm.tsx</files>

  <read_first>
    - components/admin/SpecialtyForm.tsx — read fully; DoctorForm follows the same controlled input + fetch + router.refresh pattern but with avatar and specialtyId
    - lib/supabase.ts — read fully; confirm uploadAvatar(file: File, doctorId: number): Promise<string> signature and that it throws on error
    - lib/utils.ts — confirm getInitials signature
    - .planning/phases/02-admin/02-PATTERNS.md — DoctorForm.tsx section (two-step CREATE flow, avatar preview, file change handler)
    - .planning/phases/02-admin/02-RESEARCH.md — Pattern 7 (two-step create: POST without avatar → get id → uploadAvatar → PATCH avatar), Pitfall 3 (avatar upload before doctorId exists)
    - .planning/phases/02-admin/02-UI-SPEC.md — Doctor Create/Edit Form section (fields: Nombre completo, Especialidad select, Teléfono, Duración del turno, Biografía, Foto de perfil; avatar UX; "Guardar médico" CTA; "Descartar cambios" dismiss)
  </read_first>

  <action>
    Create components/admin/DoctorForm.tsx as a Client Component.

    'use client' at top.

    Imports: useState and useRef from 'react'; useRouter from 'next/navigation'; Loader2 from 'lucide-react'; uploadAvatar from '@/lib/supabase'; getInitials from '@/lib/utils'.

    Types:
    - Doctor: { id: number; name: string; specialtyId: number; bio: string | null; phone: string | null; durationMin: number; avatar: string | null }
    - Specialty: { id: number; name: string; color: string }
    - Props: { doctor: Doctor | null; specialties: Specialty[]; onClose: () => void }

    State:
    - name, specialtyId (string for select), bio, phone, durationMin (string)
    - Initialized from doctor prop or defaults: name=doctor?.name??'', specialtyId=String(doctor?.specialtyId??''), bio=doctor?.bio??'', phone=doctor?.phone??'', durationMin=String(doctor?.durationMin??30)
    - const [preview, setPreview] = useState<string | null>(doctor?.avatar ?? null)
    - const [file, setFile] = useState<File | null>(null)
    - const [loading, setLoading] = useState(false)
    - const [error, setError] = useState('')
    - const [avatarError, setAvatarError] = useState('')
    - const fileInputRef = useRef<HTMLInputElement>(null)

    handleFileChange: e => { const selected = e.target.files?.[0]; if (!selected) return; if (selected.size > 2 * 1024 * 1024) { setAvatarError('La imagen no pudo subirse. Intentá con otro archivo.'); return }; setPreview(URL.createObjectURL(selected)); setFile(selected); setAvatarError('') }

    handleSubmit (async):
    - e.preventDefault()
    - Validate: if (!name.trim() || !specialtyId) { setError('Nombre y especialidad son requeridos'); return }
    - setLoading(true); setError(''); setAvatarError('')
    - try block:

    EDIT flow (doctor !== null):
    - Build updateData: { name: name.trim(), specialtyId: Number(specialtyId), bio: bio || null, phone: phone || null, durationMin: Number(durationMin) }
    - If file is selected: upload first: const avatarUrl = await uploadAvatar(file, doctor.id); then add avatar to updateData
    - fetch PUT /api/admin/doctors/doctor.id with updateData
    - If !res.ok: parse and setError
    - On success: onClose(); router.refresh()

    CREATE flow (doctor === null):
    - Step 1: POST /api/admin/doctors with { name, specialtyId: Number(specialtyId), bio: bio || null, phone: phone || null, durationMin: Number(durationMin) } (no avatar)
    - If !res.ok: parse and setError; return (doctor not created)
    - const { doctor: created } = await res.json()
    - Step 2: if file, try: const avatarUrl = await uploadAvatar(file, created.id); then fetch PUT /api/admin/doctors/created.id with { avatar: avatarUrl }. On upload/PATCH error: setAvatarError('La imagen no pudo subirse. Intentá con otro archivo.') — but doctor IS created; continue to close/refresh
    - onClose(); router.refresh()

    catch (for outer try): setError('Ocurrió un error al guardar. Intentá nuevamente.')
    finally: setLoading(false)

    JSX: form with onSubmit={handleSubmit}, className="flex flex-col gap-4"

    Fields in order per UI-SPEC:
    1. Avatar area (top of form): div with preview circle (64x64) and "Subir foto" trigger button. Preview: if preview, img h-16 w-16 rounded-full object-cover; else div h-16 w-16 rounded-full bg-primary/20 flex items-center justify-center text-lg font-semibold text-primary showing getInitials(name || 'M'). Below circle: button type="button" onClick={() => fileInputRef.current?.click()} className="text-sm text-accent hover:text-primary" text "Subir foto". Hidden input: input type="file" ref={fileInputRef} accept="image/*" className="hidden" onChange={handleFileChange}. If avatarError: p className="text-xs text-error" showing avatarError.
    2. Nombre completo (required): label "Nombre completo", input type="text" maxLength={120}
    3. Especialidad (required): label "Especialidad", select with options from specialties prop (value="" disabled hidden: "Seleccioná una especialidad"). Select style: same border/radius/focus as text input.
    4. Teléfono (optional): label "Teléfono", input type="tel" maxLength={20}
    5. Duración del turno (required): label "Duración del turno (minutos)", input type="number" min="10" max="120" step="5"
    6. Biografía (optional): label "Biografía", textarea rows={4} maxLength={1000}

    All labels: className="block text-xs text-text-secondary mb-1"
    All inputs/textarea/select: className="w-full rounded-lg border border-border bg-white px-3 py-2 text-sm text-text-primary focus:outline-none focus:border-[rgba(13,148,136,0.4)] focus:ring-2 focus:ring-[rgba(13,148,136,0.2)] transition-all"

    Error display: if error, p className="rounded-lg bg-red-50 px-4 py-2.5 text-sm text-error"

    Footer: same two-button pattern as SpecialtyForm — "Descartar cambios" (secondary, type="button", onClick={onClose}) + "Guardar médico" (primary, type="submit", disabled={loading}, loading spinner).

    Typography: only font-semibold (600) and regular (400). No font-medium or font-bold.
    CTA exact copy: "Guardar médico". Dismiss exact copy: "Descartar cambios".
  </action>

  <verify>
    <automated>cd /c/Users/UD/Desktop/TurneroMedico/turnero-consultorio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - components/admin/DoctorForm.tsx starts with 'use client'
    - CREATE flow: POST /api/admin/doctors called first (no avatar); then uploadAvatar called with created.id; then PUT avatar. File is NOT uploaded before doctor exists.
    - EDIT flow: if file exists, uploadAvatar called with doctor.id before PUT request
    - Avatar preview uses URL.createObjectURL(selected) for client-side preview
    - Initials fallback rendered when preview is null (getInitials used)
    - File size > 2MB triggers avatarError message "La imagen no pudo subirse. Intentá con otro archivo."
    - submit button text: "Guardar médico"
    - Dismiss button text: "Descartar cambios" with type="button"
    - Specialidad uses select element with options from specialties prop
    - Duración uses input type="number" with min="10" max="120" step="5"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>DoctorForm handles create (two-step avatar flow) and edit. Avatar preview, file validation, loading/error feedback. SC-3 and SC-5 satisfied for doctors module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST/PUT /api/admin/doctors | Mutation endpoints with JWT auth |
| Browser → Supabase Storage | uploadAvatar uploads file directly from browser using anon key; path is controlled by server-validated doctorId |
| Server Component → Middleware headers | page.tsx reads x-user-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | uploadAvatar — path injection via doctorId | mitigate | doctorId in path comes from API response (server-validated); Supabase anon key limits bucket access to 'turnero' bucket only |
| T-02-06-02 | Elevation of Privilege | DoctorsList — role bypass | mitigate | CRUD buttons excluded from JSX for RECEPTIONIST; API returns 403 for mutations regardless |
| T-02-06-03 | Tampering | DoctorForm — avatar overwrite without auth | mitigate | PUT /api/admin/doctors/[id] verifies requireAdmin before accepting avatar field; anon Supabase key cannot bypass API auth |
| T-02-06-04 | Information Disclosure | Doctor avatar URL in public Supabase Storage | accept | Avatars are non-sensitive (doctor photos); public bucket is intended per lib/supabase.ts (getPublicUrl) |
| T-02-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan |
</threat_model>

<verification>
After both tasks:
- npx tsc --noEmit passes
- grep -c 'use client' app/(admin)/medicos/page.tsx returns 0
- grep -c 'Promise.all' app/(admin)/medicos/page.tsx returns 1
- grep -c 'aria-label="Editar médico"' components/admin/DoctorsList.tsx returns 1
- grep -c 'aria-label="Eliminar médico"' components/admin/DoctorsList.tsx returns 1
- grep -c 'uploadAvatar' components/admin/DoctorForm.tsx returns at least 2 (import + call)
- grep -c 'URL.createObjectURL' components/admin/DoctorForm.tsx returns 1
- grep -c 'Guardar médico' components/admin/DoctorForm.tsx returns 1
</verification>

<success_criteria>
- Navigating to /admin/medicos shows doctors list with avatars/initials, specialty chips, phone, duration
- ADMIN: "Crear médico" button, edit/delete per row; RECEPTIONIST: list + Disponibilidad link only
- Create doctor: fill form, optionally select avatar (shows preview), save → list updates
- Avatar upload happens AFTER doctor is created (two-step flow; doctor created even if avatar fails)
- Edit doctor: drawer pre-filled; upload new avatar or keep existing
- Delete: inline confirm with "¿Confirmar eliminación? Esta acción no se puede deshacer."
- Loading spinner during save; error message on failure
</success_criteria>

<output>
Create `.planning/phases/02-admin/02-06-SUMMARY.md` when done
</output>
