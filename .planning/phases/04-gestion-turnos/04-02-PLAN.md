---
phase: 04-gestion-turnos
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/admin/AppointmentsList.tsx
autonomous: true
requirements:
  - TURNO-01
  - TURNO-02
  - TURNO-03
  - TURNO-04
  - TURNO-05

must_haves:
  truths:
    - "AppointmentsList renders a table with 5 columns: Paciente+DNI, Médico, Fecha+Hora, Estado, Acciones (D-01, D-02)"
    - "Filter bar with date (default today), médico select, estado select, DNI input triggers refetch on change (D-03, D-04)"
    - "DNI text input debounced 300ms before firing fetch"
    - "PENDING rows show Confirmar and Cancelar buttons; Cancelar expands inline confirm before firing PATCH (D-01)"
    - "CONFIRMED rows show WhatsApp link with MessageCircle icon; clicking fires PATCH /whatsapp fire-and-forget and swaps to MessageCircleCheck teal (D-05, D-06, D-07)"
    - "Argentina phone normalization: strip leading 0, prepend 549 in wa.me URL"
    - "Empty state shows Calendar icon + 'Sin turnos para este filtro' when no results"
    - "Loading state shows 'Cargando...' text during filter-triggered fetch (filter bar remains visible)"
    - "Error state shows 'No se pudieron cargar los turnos. Recargá la página.' on fetch failure"
  artifacts:
    - path: "components/admin/AppointmentsList.tsx"
      provides: "Client Component owning filter state, appointments state, status mutations, WA tracking"
      exports: ["AppointmentsList"]
      contains: "use client"
  key_links:
    - from: "components/admin/AppointmentsList.tsx"
      to: "/api/admin/appointments"
      via: "useEffect fetch on filter change with URLSearchParams"
      pattern: "api/admin/appointments"
    - from: "components/admin/AppointmentsList.tsx"
      to: "/api/admin/appointments/[id]"
      via: "PATCH fetch in handleStatusChange"
      pattern: "api/admin/appointments.*PATCH"
    - from: "components/admin/AppointmentsList.tsx"
      to: "lib/utils.ts"
      via: "cn, formatDate, formatTime, formatDni, STATUS_LABELS, STATUS_COLORS"
      pattern: "from '@/lib/utils'"
---

<objective>
Build the AppointmentsList client component — the interactive core of Phase 4. This single file contains the filter bar, appointment table, status mutation handlers, and WhatsApp integration. It receives initialData and doctors from the Server Component page (Plan 04-03) as props, owns filter state client-side, and re-fetches from the API on filter change without page reload.

Purpose: Deliver the complete management UI: list, filter, status change, and WhatsApp link with optimistic tracking.

Output: components/admin/AppointmentsList.tsx. TypeScript build passes. Manual browser verification confirms all interactions from VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-gestion-turnos/04-CONTEXT.md
@.planning/phases/04-gestion-turnos/04-RESEARCH.md
@.planning/phases/04-gestion-turnos/04-UI-SPEC.md
@.planning/phases/04-gestion-turnos/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppointmentsList — filter state, fetch logic, and type definitions (D-03, D-04, TURNO-01, TURNO-05)</name>
  <files>components/admin/AppointmentsList.tsx</files>
  <read_first>
    - components/admin/SpecialtiesList.tsx — full file: 'use client' directive, state shape, router pattern, table classes, inline confirm pattern (deletingId), bg-surface rounded-xl shadow-card wrapper, border-b border-border last:border-b-0 row class
    - lib/utils.ts — full file: cn, formatDate, formatTime, formatDni, STATUS_LABELS, STATUS_COLORS exports
    - app/globals.css — design tokens: bg-background, bg-surface, text-text-primary, text-text-secondary, text-text-muted, text-accent, text-error, border-border, shadow-card, font-display
    - prisma/schema.prisma — Appointment fields for local type definition: id, doctorId, patientName, patientDni, patientPhone, patientEmail, patientInsurance, date, time, status, notes, whatsappSent; AppointmentStatus enum values
  </read_first>
  <action>
    Create components/admin/AppointmentsList.tsx.

    Top of file: 'use client' directive.

    Imports: useState, useEffect, useCallback, useRef from 'react'; MessageCircle, MessageCircleCheck, Calendar from 'lucide-react'; cn, formatDate, formatTime, formatDni, STATUS_LABELS, STATUS_COLORS from '@/lib/utils'.

    Define local types (do not import from Prisma client in a client component — define inline):
    - type AppointmentStatus = 'PENDING' | 'CONFIRMED' | 'CANCELLED' | 'COMPLETED'
    - type Doctor = { id: number; name: string }
    - type Appointment = { id: number; doctorId: number; patientName: string; patientDni: string; patientPhone: string; patientEmail: string | null; patientInsurance: string | null; date: string; time: string; status: AppointmentStatus; notes: string | null; whatsappSent: boolean; doctor: Doctor }

    Props interface: AppointmentsListProps { initialData: Appointment[]; doctors: Doctor[]; role: 'ADMIN' | 'RECEPTIONIST' }

    State:
    - appointments: Appointment[] — initialize from initialData
    - loading: boolean — false initially
    - error: string | null — null initially
    - filters: { date: string; doctorId: string; status: string; dni: string } — initialize date to today's ISO string: new Date().toISOString().split('T')[0]
    - cancellingId: number | null — for inline cancel confirm
    - loadingId: number | null — for per-row PATCH loading state

    useEffect that fires when filters.date, filters.doctorId, filters.status change (immediate fetch — no debounce for selects/date). For filters.dni, use a separate useEffect with a 300ms debounce via setTimeout/clearTimeout using a useRef<ReturnType<typeof setTimeout> | null>(null).

    fetchAppointments function (useCallback, deps: filters): builds URLSearchParams from non-empty filter values, calls GET /api/admin/appointments, sets appointments state from response. On error: sets error state. Sets loading true before fetch, false after.

    Note: Do NOT call router.refresh() anywhere in this component — update appointments state directly to avoid losing filter state (per RESEARCH.md Pitfall 4, Anti-Patterns).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      Navigate /admin/turnos (after Plan 04-03 is done). Change the Médico select — table updates without page reload. Type a DNI — table updates after 300ms pause.
    </human-check>
  </verify>
  <acceptance_criteria>
    - File starts with 'use client' on line 1.
    - AppointmentsList is a named export (not default).
    - Appointment type includes all fields from schema: id, doctorId, patientName, patientDni, patientPhone, patientEmail, patientInsurance, date (string), time, status, notes, whatsappSent, doctor.
    - filters state initializes date to new Date().toISOString().split('T')[0] — grep confirms "toISOString().split".
    - fetchAppointments uses URLSearchParams and calls /api/admin/appointments.
    - DNI change has 300ms debounce via setTimeout — grep confirms "300" and "clearTimeout".
    - No router.refresh() call anywhere in the file — grep confirms absence.
    - npm run build passes.
  </acceptance_criteria>
  <done>AppointmentsList component compiles with correct state shape and fetch logic. Filter state initializes to today's date. DNI input is debounced.</done>
</task>

<task type="auto">
  <name>Task 2: Table render, status actions, WhatsApp integration (D-01, D-02, D-05, D-06, D-07, TURNO-02, TURNO-03, TURNO-04)</name>
  <files>components/admin/AppointmentsList.tsx</files>
  <read_first>
    - components/admin/AppointmentsList.tsx — current state from Task 1 (fetch/state layer)
    - .planning/phases/04-gestion-turnos/04-UI-SPEC.md — Component Inventory section: filter bar container class, table wrapper class, thead th class, tbody tr class, td class, status badge, Acciones column per-status, empty state, loading state, error state, copywriting contract
    - .planning/phases/04-gestion-turnos/04-RESEARCH.md — Pattern 4 (WhatsApp URL), Pattern 5 (optimistic whatsappSent), Pattern 6 (inline cancel), Anti-Patterns section
    - lib/utils.ts — STATUS_LABELS, STATUS_COLORS keys for badge rendering
  </read_first>
  <action>
    Continue editing components/admin/AppointmentsList.tsx — add the render section and action handlers.

    handleStatusChange(id: number, status: string): sets loadingId(id), fires PATCH /api/admin/appointments/${id} with body { status }, on success updates appointments state via setAppointments(prev => prev.map(a => a.id === id ? { ...a, ...data.appointment } : a)), clears loadingId and cancellingId. Do NOT call router.refresh().

    buildWhatsAppUrl(appointment: Appointment): strips leading 0 from patientPhone with replace(/^0/, ''), builds message using template from D-06: `Hola ${appointment.patientName}, tu turno con ${appointment.doctor.name} el ${formatDate(appointment.date)} a las ${formatTime(appointment.time)} ha sido CONFIRMADO.`, returns `https://wa.me/549${phone}?text=${encodeURIComponent(message)}`.

    handleWhatsAppClick(id: number): optimistically sets whatsappSent:true in appointments state via setAppointments(prev => prev.map(a => a.id === id ? { ...a, whatsappSent: true } : a)), then fires fetch PATCH /api/admin/appointments/${id}/whatsapp without await (fire-and-forget, .catch(console.error)).

    JSX structure:

    Filter bar: div className="flex flex-wrap items-center gap-3 mb-6". Contains:
    - input type="date" className={inputClass} value={filters.date} onChange updates filters.date
    - select className={cn(inputClass, 'w-48')} value={filters.doctorId} onChange updates filters.doctorId. Options: <option value="">Todos los médicos</option> + doctors.map
    - select className={cn(inputClass, 'w-40')} value={filters.status} onChange updates filters.status. Options: <option value="">Todos los estados</option> + ['PENDING','CONFIRMED','CANCELLED','COMPLETED'].map using STATUS_LABELS
    - input type="text" className={cn(inputClass, 'w-40')} placeholder="Buscar por DNI" value={filters.dni} onChange updates filters.dni

    inputClass constant: "rounded-lg border border-border bg-surface px-3 py-2 text-sm text-text-primary focus:border-[rgba(20,184,166,0.4)] focus:outline-none focus:ring-1 focus:ring-[rgba(20,184,166,0.4)]"

    Loading state: if loading render <div className="py-8 text-center text-sm text-text-secondary">Cargando...</div> — keep filter bar visible above it.

    Error state: if error render <div className="py-8 text-center text-sm text-text-secondary">No se pudieron cargar los turnos. Recargá la página.</div>

    Table: div className="bg-surface rounded-xl shadow-card". Inside: table className="w-full table-auto". thead: border-b border-border. Five th elements: "PACIENTE", "MÉDICO", "FECHA Y HORA", "ESTADO", "ACCIONES". th className: "px-6 py-3 text-left text-xs font-semibold text-text-muted uppercase tracking-wide". Last th (Acciones): add "w-40 text-right". Estado th: add "w-32".

    tbody: appointments.map each row. tr className: "border-b border-border last:border-b-0 hover:bg-background transition-colors". td className: "px-6 py-4 align-top".

    Paciente+DNI cell: <p className="text-sm font-semibold text-text-primary">{appointment.patientName}</p> + <p className="text-xs text-text-secondary">{formatDni(appointment.patientDni)}</p>

    Médico cell: <span className="text-sm text-text-primary">{appointment.doctor.name}</span>

    Fecha+Hora cell: <p className="text-sm text-text-primary">{formatDate(appointment.date)}</p> + <p className="text-xs text-text-secondary">{formatTime(appointment.time)}</p>

    Estado cell: <span className={cn('inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold', STATUS_COLORS[appointment.status])}>{STATUS_LABELS[appointment.status]}</span>

    Acciones cell (className="px-6 py-4 align-top w-40"): render based on appointment.status:

    PENDING: if cancellingId === appointment.id show inline confirm: <span className="text-xs text-text-secondary">¿Cancelar este turno?</span> + <button className="text-sm font-semibold text-error" onClick={() => handleStatusChange(appointment.id, 'CANCELLED')}>Sí, cancelar</button> + <button className="text-sm text-text-secondary" onClick={() => setCancellingId(null)}>No</button>. If not cancelling: <div className="flex items-center justify-end gap-2"><button className="text-sm font-semibold text-teal-700 hover:text-teal-900 transition-colors" disabled={loadingId===appointment.id} onClick={() => handleStatusChange(appointment.id, 'CONFIRMED')}>Confirmar</button><button className="text-sm font-semibold text-error hover:text-red-700 transition-colors" disabled={loadingId===appointment.id} onClick={() => setCancellingId(appointment.id)}>Cancelar</button></div>. When loadingId===appointment.id add opacity-50 cursor-not-allowed.

    CONFIRMED: <a href={buildWhatsAppUrl(appointment)} target="_blank" rel="noopener noreferrer" onClick={() => handleWhatsAppClick(appointment.id)} className="flex items-center justify-end gap-1 text-sm text-accent hover:underline">{appointment.whatsappSent ? <MessageCircleCheck className="h-4 w-4 text-accent" /> : <MessageCircle className="h-4 w-4 text-accent" />}{appointment.whatsappSent ? 'WA enviado' : 'Enviar WA'}</a>

    CANCELLED and COMPLETED: <span className="text-xs text-text-muted">—</span>

    Empty state (appointments.length === 0, not loading): render inside table container: <div className="flex flex-col items-center justify-center py-12 text-center"><Calendar className="h-10 w-10 text-text-muted mb-3" /><p className="text-sm font-semibold text-text-primary mb-1">Sin turnos para este filtro</p><p className="text-sm text-text-secondary">Probá con otra fecha, médico o estado.</p></div>
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      With seed data: navigate /admin/turnos.
      1. Table loads with today's appointments.
      2. Click Confirmar on a PENDING row — badge changes to Confirmado, WhatsApp link appears.
      3. Click Enviar WA — icon swaps to MessageCircleCheck, label changes to "WA enviado".
      4. Click Cancelar on a PENDING row — inline confirm appears. Click "Sí, cancelar" — badge changes to Cancelado, dash shows in Acciones.
      5. Change Médico select — table filters without reload.
      6. Clear date field — shows up to 200 appointments across all dates.
      7. Empty state shows when filter returns no results.
    </human-check>
  </verify>
  <acceptance_criteria>
    - AppointmentsList renders a table with exactly 5 column headers: PACIENTE, MÉDICO, FECHA Y HORA, ESTADO, ACCIONES.
    - Status badge uses cn() with STATUS_COLORS[appointment.status] — grep confirms "STATUS_COLORS[".
    - WhatsApp anchor href contains "wa.me/549" — grep confirms "wa.me/549".
    - buildWhatsAppUrl uses encodeURIComponent on message — grep confirms "encodeURIComponent".
    - Phone normalization strips leading 0: replace(/^0/, '') present in buildWhatsAppUrl — grep confirms "replace(/\^0".
    - Optimistic update swaps whatsappSent to true in state before server confirms — handleWhatsAppClick calls setAppointments before fetch.
    - handleWhatsAppClick fetch is fire-and-forget (no await before setAppointments, no await before window open or href nav).
    - Inline cancel confirm pattern: cancellingId state present, setCancellingId called on Cancelar click.
    - CANCELLED and COMPLETED rows show dash span, not action buttons.
    - Empty state contains text "Sin turnos para este filtro" — grep confirms.
    - npm run build passes.
  </acceptance_criteria>
  <done>AppointmentsList fully implemented with table, filter bar, all status-contextual actions, WhatsApp link with optimistic tracking, empty/loading/error states. All decisions D-01 through D-07 implemented. TypeScript build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser | Admin user interacts with client component — no direct DB access |
| browser → API | All mutations go through authenticated Route Handlers (Plan 04-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | WhatsApp URL (patientPhone) | mitigate | encodeURIComponent on message; phone from DB (digits only); Argentina prefix hardcoded as "549" — no user input in URL construction |
| T-04-07 | Tampering | Status change (client-side) | accept | Client sends PATCH to authenticated Route Handler — server validates status enum; client optimism only affects local UI display |
| T-04-08 | Information Disclosure | Appointment data in client | accept | Page is behind admin middleware — only authenticated ADMIN users reach this component |
| T-04-SC | Tampering | npm installs | accept | No new packages — all imports from pre-existing lucide-react and lib/utils.ts |
</threat_model>

<verification>
- npm run build exits 0
- /admin/turnos loads with today's appointments on first render
- All four filter controls change table contents without page reload
- DNI input only fires fetch after 300ms pause
- PENDING row shows Confirmar + Cancelar buttons
- Clicking Confirmar updates badge to Confirmado and shows WhatsApp link immediately
- Clicking Cancelar shows inline "¿Cancelar este turno?" confirm before any PATCH
- Clicking Enviar WA opens wa.me link AND swaps icon/label optimistically
- CANCELLED and COMPLETED rows show dash
- Empty state displays when no appointments match filters
</verification>

<success_criteria>
AppointmentsList.tsx exists, compiles, and implements all locked decisions (D-01 through D-07). Full coverage: TURNO-01 (list+filters), TURNO-02 (status change), TURNO-03 (whatsappSent tracking), TURNO-04 (WhatsApp link), TURNO-05 (filter by date/médico/estado/DNI).
</success_criteria>

<output>
Create .planning/phases/04-gestion-turnos/04-02-SUMMARY.md when done.
</output>
