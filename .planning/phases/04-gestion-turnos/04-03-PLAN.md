---
phase: 04-gestion-turnos
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/(admin)/admin/turnos/page.tsx
autonomous: true
requirements:
  - TURNO-01
  - TURNO-04
  - TURNO-05

must_haves:
  truths:
    - "Navigating /admin/turnos shows today's appointments on first load without client-side waterfall (D-04)"
    - "Server Component passes initialData (today's appointments with doctor relation) and doctors list to AppointmentsList"
    - "AppointmentsList receives role from middleware x-user-role header"
    - "Admin sidebar /admin/turnos nav item becomes active (route exists)"
    - "TypeScript build passes with no errors"
  artifacts:
    - path: "app/(admin)/admin/turnos/page.tsx"
      provides: "Server Component initial fetch for today's appointments + doctors list"
      contains: "export default async function TurnosPage"
  key_links:
    - from: "app/(admin)/admin/turnos/page.tsx"
      to: "components/admin/AppointmentsList"
      via: "<AppointmentsList initialData={appointments} doctors={doctors} role={role} />"
      pattern: "AppointmentsList"
    - from: "app/(admin)/admin/turnos/page.tsx"
      to: "prisma.appointment.findMany"
      via: "Promise.all with today date (local constructor)"
      pattern: "prisma\\.appointment\\.findMany"
---

<objective>
Create the turnos Server Component page that bootstraps the appointment management view. It reads the user role from the middleware-injected x-user-role header, fetches today's appointments and active doctors list in parallel via Promise.all, and renders AppointmentsList with that data as props. This is the entry point that connects the admin layout, API layer (Plan 04-01), and client component (Plan 04-02).

Purpose: Eliminate client-side waterfall on first load — today's appointments arrive pre-rendered. Activate the /admin/turnos nav entry already declared in AdminSidebar.

Output: app/(admin)/admin/turnos/page.tsx. TypeScript build passes. /admin/turnos renders without errors and shows today's appointments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-gestion-turnos/04-CONTEXT.md
@.planning/phases/04-gestion-turnos/04-RESEARCH.md
@.planning/phases/04-gestion-turnos/04-UI-SPEC.md
@.planning/phases/04-gestion-turnos/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: turnos/page.tsx — Server Component with parallel initial fetch (D-04, TURNO-01, TURNO-05)</name>
  <files>app/(admin)/admin/turnos/page.tsx</files>
  <read_first>
    - app/(admin)/admin/medicos/page.tsx — Promise.all pattern with two Prisma queries, await headers(), x-user-role, p-6 wrapper, font-display heading
    - app/(admin)/admin/especialidades/page.tsx — simpler single-query pattern for comparison
    - components/admin/AppointmentsList.tsx — the component this page renders; verify prop names match: initialData, doctors, role
    - prisma/schema.prisma — Appointment model (date @db.Date for today query); Doctor model (isActive, name fields)
    - components/admin/AdminSidebar.tsx — confirm /admin/turnos is already declared as a nav item (no change needed)
  </read_first>
  <action>
    Create app/(admin)/admin/turnos/page.tsx as an async Server Component (no 'use client').

    Imports: headers from 'next/headers', prisma from '@/lib/prisma', AppointmentsList from '@/components/admin/AppointmentsList'.

    Inside TurnosPage():
    - const headersList = await headers() — MANDATORY await per AGENTS.md.
    - const role = headersList.get('x-user-role') as 'ADMIN' | 'RECEPTIONIST'

    Build today's date for the initial Prisma query using the local constructor to avoid UTC mismatch (same rule as GET route handler):
    - const today = new Date(); today.setHours(0, 0, 0, 0)

    Fetch in parallel with Promise.all:
    - prisma.appointment.findMany({ where: { date: today }, orderBy: [{ date: 'asc' }, { time: 'asc' }], include: { doctor: { select: { id: true, name: true } } } })
    - prisma.doctor.findMany({ where: { isActive: true }, orderBy: { name: 'asc' }, select: { id: true, name: true } })

    Return JSX:
    <div className="p-6">
      <div className="mb-6">
        <h1 className="font-display text-xl font-semibold text-text-primary">Turnos</h1>
      </div>
      <AppointmentsList initialData={appointments} doctors={doctors} role={role} />
    </div>

    Do NOT import or use NextResponse, useRouter, useState, or any client-side hooks — this is a Server Component.
    Do NOT pass a default date string prop to AppointmentsList — the client component initializes its own date state from new Date() directly (per Plan 04-02 Task 1).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      1. Navigate /admin/turnos — page renders with heading "Turnos" and the appointment table pre-populated with today's data.
      2. The sidebar /admin/turnos link is active (teal highlight).
      3. No React hydration error in browser console.
      4. Change the Médico select — table re-fetches without page reload (confirms client component layer works end-to-end).
    </human-check>
  </verify>
  <acceptance_criteria>
    - File app/(admin)/admin/turnos/page.tsx exists.
    - Exports default async function TurnosPage (no 'use client').
    - Contains "await headers()" — grep confirms this pattern (mandatory Next.js 16 convention per AGENTS.md).
    - Uses Promise.all for the two Prisma queries — grep confirms "Promise.all".
    - today date built with setHours(0, 0, 0, 0) — grep confirms "setHours".
    - Renders <AppointmentsList initialData={appointments} doctors={doctors} role={role} />.
    - Page heading matches copywriting contract: "Turnos" (not "Gestión de Turnos").
    - npm run build exits 0 with no TypeScript errors.
    - /admin/turnos route is accessible (Next.js registers it under (admin) route group).
  </acceptance_criteria>
  <done>turnos/page.tsx exists as a Server Component, pre-fetches today's data, and renders AppointmentsList with correct props. /admin/turnos is reachable and shows the appointment management view. Phase 4 is complete end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → Server Component | x-user-role header injected by middleware after JWT verification — Server Component trusts this header for role-based rendering only |
| Server Component → DB | Direct Prisma query — no user input, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Elevation of Privilege | /admin/turnos route | accept | Middleware already redirects unauthenticated requests to /admin/login before reaching this page; no additional guard needed in the Server Component itself |
| T-04-10 | Information Disclosure | Initial appointments SSR | accept | Data is served only to authenticated admin sessions; middleware enforces auth before Server Component executes |
| T-04-SC | Tampering | npm installs | accept | No new packages — page imports only from existing prisma, next/headers, and the component created in Plan 04-02 |
</threat_model>

<verification>
- npm run build exits 0 (no TypeScript errors across all Phase 4 files)
- /admin/turnos renders without 404 or 500
- Page heading "Turnos" visible
- Today's appointments pre-rendered on first load (no loading spinner on initial paint)
- Filter bar interactive: changing filters re-fetches without page reload
- Sidebar /admin/turnos link highlights as active
</verification>

<success_criteria>
app/(admin)/admin/turnos/page.tsx exists and wire all Phase 4 layers together: middleware auth → Server Component SSR fetch → AppointmentsList client component → API Route Handlers. All four ROADMAP Phase 4 success criteria met: (1) lista+filtros, (2) cambio de estado, (3) búsqueda por DNI, (4) WhatsApp link. npm run build passes.
</success_criteria>

<output>
Create .planning/phases/04-gestion-turnos/04-03-SUMMARY.md when done.
</output>
