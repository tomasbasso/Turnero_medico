---
phase: 04-gestion-turnos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/admin/appointments/route.ts
  - app/api/admin/appointments/[id]/route.ts
  - app/api/admin/appointments/[id]/whatsapp/route.ts
autonomous: true
requirements:
  - TURNO-01
  - TURNO-02
  - TURNO-03
  - TURNO-05

must_haves:
  truths:
    - "GET /api/admin/appointments returns appointments filtered by date, doctorId, status, dni (all optional)"
    - "PATCH /api/admin/appointments/[id] updates status to CONFIRMED, CANCELLED, or COMPLETED"
    - "PATCH /api/admin/appointments/[id]/whatsapp sets whatsappSent=true"
    - "All three routes reject unauthenticated requests with 401 and non-ADMIN roles with 403"
    - "Date filter avoids UTC timezone mismatch by using local Date constructor"
  artifacts:
    - path: "app/api/admin/appointments/route.ts"
      provides: "GET list with conditional Prisma where, includes doctor relation"
      exports: ["GET"]
    - path: "app/api/admin/appointments/[id]/route.ts"
      provides: "PATCH status update with P2025 guard"
      exports: ["PATCH"]
    - path: "app/api/admin/appointments/[id]/whatsapp/route.ts"
      provides: "PATCH whatsappSent single-field update"
      exports: ["PATCH"]
  key_links:
    - from: "app/api/admin/appointments/route.ts"
      to: "prisma.appointment.findMany"
      via: "conditional where object built from searchParams"
      pattern: "prisma\\.appointment\\.findMany"
    - from: "app/api/admin/appointments/[id]/route.ts"
      to: "prisma.appointment.update"
      via: "await params — id destructured from Promise"
      pattern: "await params"
---

<objective>
Create the three Route Handlers that back the Phase 4 appointment management UI: GET list with four optional filters, PATCH status update, and PATCH whatsappSent flag. These handlers are the complete API layer for Phase 4 — no new schema changes, no new dependencies.

Purpose: Provide authenticated, validated endpoints the AppointmentsList client component will call.

Output: Three new files under app/api/admin/appointments/. TypeScript build passes. Manual curl verification confirms auth guard, filter behavior, and status update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-gestion-turnos/04-CONTEXT.md
@.planning/phases/04-gestion-turnos/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/admin/appointments — list with conditional filters (D-03, TURNO-01, TURNO-05)</name>
  <files>app/api/admin/appointments/route.ts</files>
  <read_first>
    - app/api/admin/specialties/route.ts — GET list pattern: requireAdmin, Response.json, prisma query
    - lib/auth-helpers.ts — requireAdmin signature: returns { payload } | Response; check instanceof Response
    - lib/prisma.ts — singleton import alias
    - prisma/schema.prisma — Appointment model fields: id, doctorId, patientName, patientDni, patientPhone, patientEmail, patientInsurance, date @db.Date, time, status, notes, whatsappSent; Doctor relation; @@index([date]), @@index([status]), @@index([patientDni])
  </read_first>
  <action>
    Create app/api/admin/appointments/route.ts exporting GET(request: NextRequest).

    Auth: call requireAdmin(request); if instanceof Response return it immediately.

    Parse four optional query params from request.nextUrl.searchParams: date (string "YYYY-MM-DD" or null), doctorId (numeric string or null), status (enum string or null), dni (partial string or null).

    Build conditional where object typed as Prisma.AppointmentWhereInput (import from @prisma/client). Add each filter only if the param is non-empty:
    - date: construct with new Date(year, month-1, day) — split "YYYY-MM-DD" on "-", parse each part as Number. DO NOT use new Date("YYYY-MM-DD") (UTC parse causes timezone mismatch per D-04 pitfall, TURNO-05).
    - doctorId: Number(doctorId); skip if isNaN.
    - status: string equality; skip if empty.
    - patientDni: { contains: dni } for partial match.

    Query: prisma.appointment.findMany({ where, orderBy: [{ date: 'asc' }, { time: 'asc' }], include: { doctor: { select: { id: true, name: true } } } }).

    Add a safety cap: take: 200 to prevent unbounded queries when no date filter is applied.

    Return Response.json({ appointments }) — NOT NextResponse.json (no cookies set here).

    Import: NextRequest from 'next/server', requireAdmin from '@/lib/auth-helpers', prisma from '@/lib/prisma'. Also import Prisma namespace from '@prisma/client' if needed for typing, or use Record<string, unknown> for the where object.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      With dev server running: GET /api/admin/appointments without cookie returns 401.
      GET /api/admin/appointments?date=TODAY_ISO with valid admin cookie returns { appointments: [...] } with doctor.name included.
    </human-check>
  </verify>
  <acceptance_criteria>
    - File app/api/admin/appointments/route.ts exists and exports GET.
    - GET without auth cookie returns JSON { error: 'No autenticado' } with status 401.
    - GET with valid admin cookie and no params returns { appointments: [] } or an array (no 500).
    - GET with date param uses new Date(year, month-1, day) constructor — grep confirms "new Date(year" or equivalent 3-arg constructor, NOT "new Date(date)" direct string parse.
    - Response.json is used, not NextResponse.json — grep shows "Response.json" and no import of NextResponse.
    - doctor relation included in findMany — grep confirms "include" and "doctor".
    - take: 200 present as safety cap.
  </acceptance_criteria>
  <done>GET /api/admin/appointments responds with filtered appointment list. TypeScript build passes. Auth guard returns 401/403 for unauthenticated/non-admin requests.</done>
</task>

<task type="auto">
  <name>Task 2: PATCH /api/admin/appointments/[id] (status) + PATCH /api/admin/appointments/[id]/whatsapp (D-07, TURNO-02, TURNO-03)</name>
  <files>
    app/api/admin/appointments/[id]/route.ts
    app/api/admin/appointments/[id]/whatsapp/route.ts
  </files>
  <read_first>
    - app/api/admin/doctors/[id]/route.ts — await params pattern: { params }: { params: Promise<{ id: string }> }, const { id } = await params
    - lib/auth-helpers.ts — requireAdmin signature
    - prisma/schema.prisma — AppointmentStatus enum: PENDING, CONFIRMED, CANCELLED, COMPLETED; Appointment.whatsappSent Boolean
  </read_first>
  <action>
    Create app/api/admin/appointments/[id]/route.ts exporting PATCH.

    Signature: PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }).
    Auth: requireAdmin(request); return early if instanceof Response.
    Params: const { id } = await params — MANDATORY await (Next.js 16 requirement per AGENTS.md).
    Validate id: numericId = parseInt(id, 10); if isNaN return Response.json({ error: 'ID inválido' }, { status: 400 }).
    Parse body: body = await request.json().catch(() => null). Validate body.status is one of ['PENDING', 'CONFIRMED', 'CANCELLED', 'COMPLETED']; if not, return 400.
    Update: wrap prisma.appointment.update({ where: { id: numericId }, data: { status: body.status }, include: { doctor: { select: { id: true, name: true } } } }) in try/catch. Catch PrismaClientKnownRequestError with code 'P2025' → return Response.json({ error: 'Turno no encontrado' }, { status: 404 }). Re-throw other errors.
    Return: Response.json({ appointment }) — NOT NextResponse.json.

    Create app/api/admin/appointments/[id]/whatsapp/route.ts exporting PATCH.

    Same signature and await params pattern. Auth: requireAdmin. Validate numericId.
    No body validation needed — always sets whatsappSent: true.
    Update: prisma.appointment.update({ where: { id: numericId }, data: { whatsappSent: true } }). Wrap in try/catch for P2025 → 404.
    Return: Response.json({ ok: true }).

    Both files import NextRequest from 'next/server', requireAdmin from '@/lib/auth-helpers', prisma from '@/lib/prisma'. Import PrismaClientKnownRequestError from '@prisma/client/runtime/library' for error type check, or use (error as { code?: string }).code === 'P2025' pattern to avoid the import.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>
      With dev server: PATCH /api/admin/appointments/1 body { "status": "CONFIRMED" } with admin cookie returns updated appointment. PATCH /api/admin/appointments/1/whatsapp with admin cookie returns { ok: true } and whatsappSent becomes true in DB.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Both files exist: app/api/admin/appointments/[id]/route.ts and app/api/admin/appointments/[id]/whatsapp/route.ts.
    - Both export PATCH function with params typed as Promise<{ id: string }>.
    - Both contain "await params" — grep confirms this pattern.
    - [id]/route.ts validates status against VALID_STATUSES array containing all four enum values.
    - [id]/whatsapp/route.ts sets data: { whatsappSent: true } unconditionally.
    - Both use Response.json not NextResponse.json.
    - P2025 error caught and returned as 404 in both handlers.
    - PATCH /api/admin/appointments/999 returns 404 for non-existent id.
    - PATCH /api/admin/appointments/1 with invalid status returns 400.
  </acceptance_criteria>
  <done>Both PATCH handlers exist and compile. Status update returns updated appointment. whatsapp handler sets whatsappSent=true. Both return 401/403 without valid admin cookie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Unauthenticated browser calls these routes — all three must verify JWT |
| API → DB | Prisma queries use parameterized binding — no raw SQL injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | GET /api/admin/appointments | mitigate | requireAdmin() returns 401/403 before any Prisma query executes |
| T-04-02 | Elevation of Privilege | PATCH /api/admin/appointments/[id] | mitigate | requireAdmin() on every call; status validated against closed enum before update |
| T-04-03 | Elevation of Privilege | PATCH /api/admin/appointments/[id]/whatsapp | mitigate | requireAdmin() on every call; only sets whatsappSent=true, no body spread into Prisma data |
| T-04-04 | Tampering | GET query params (date/doctorId/status/dni) | mitigate | date split-parsed to numbers; doctorId validated isNaN; status matched to enum; dni passed as { contains } to Prisma (parameterized) — no raw SQL |
| T-04-05 | Tampering | PATCH body mass assignment | mitigate | [id]/route.ts reads only body.status; /whatsapp/route.ts ignores body entirely — no object spread into Prisma data |
| T-04-SC | Tampering | npm installs | accept | No new packages in this phase — all dependencies pre-existing and verified |
</threat_model>

<verification>
- npm run build exits 0 after creating all three files
- GET /api/admin/appointments without cookie → 401
- GET /api/admin/appointments with admin cookie → { appointments: [...] }
- GET /api/admin/appointments?date=2026-05-29 with admin cookie → filtered results (no UTC mismatch)
- PATCH /api/admin/appointments/[id] with { status: "CONFIRMED" } → updated appointment in response
- PATCH /api/admin/appointments/[id] with invalid status → 400
- PATCH /api/admin/appointments/999 (non-existent) → 404
- PATCH /api/admin/appointments/[id]/whatsapp → { ok: true }, DB row whatsappSent=true
</verification>

<success_criteria>
Three Route Handlers exist and compile without TypeScript errors. Auth guard correctly returns 401/403. Filtering builds conditional where without UTC mismatch. Status PATCH validates input and handles P2025. whatsapp PATCH sets single field. TURNO-01, TURNO-02, TURNO-03, TURNO-05 requirements satisfied at the API layer.
</success_criteria>

<output>
Create .planning/phases/04-gestion-turnos/04-01-SUMMARY.md when done.
</output>
