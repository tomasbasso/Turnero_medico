---
phase: 05-pulido-y-deploy
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - package.json
  - app/globals.css
  - components/admin/Drawer.tsx
  - components/admin/NewAppointmentModal.tsx
autonomous: false
requirements: [D-06, D-07]

must_haves:
  truths:
    - "Los tokens --success y --error en light mode cumplen contraste WCAG AA (≥4.5:1 como texto)"
    - "Todos los elementos interactivos muestran un focus ring teal visible con :focus-visible"
    - "El Drawer y el NewAppointmentModal atrapan el foco del teclado (Tab no escapa)"
    - "El Drawer y el NewAppointmentModal se cierran con la tecla Escape"
    - "El Drawer y el NewAppointmentModal exponen role=dialog y aria-modal=true a screen readers"
    - "El botón de cierre del NewAppointmentModal tiene aria-label"
  artifacts:
    - path: "app/globals.css"
      provides: "Tokens WCAG ajustados + focus-visible ring global"
      contains: ":focus-visible"
    - path: "components/admin/Drawer.tsx"
      provides: "Focus trap + Escape + role dialog"
      contains: "aria-modal"
    - path: "components/admin/NewAppointmentModal.tsx"
      provides: "Focus trap + Escape + role dialog + aria-label en close"
      contains: "aria-modal"
  key_links:
    - from: "components/admin/Drawer.tsx"
      to: "focus-trap-react"
      via: "import FocusTrap + wrapper del panel"
      pattern: "FocusTrap"
    - from: "components/admin/NewAppointmentModal.tsx"
      to: "focus-trap-react"
      via: "import FocusTrap + wrapper del panel + Escape useEffect"
      pattern: "FocusTrap"
---

<objective>
Cerrar la auditoría WCAG AA de pulido (D-06, D-07): ajustar los tokens de contraste que fallan AA (`--success`, `--error` en light), agregar focus ring teal visible global con `:focus-visible`, y dar manejo de teclado/screen-reader a los dos overlays del proyecto (Drawer y NewAppointmentModal) con focus trap, Escape, `role="dialog"`, `aria-modal="true"` y `aria-label` en el botón de cierre del modal.

Purpose: Accesibilidad AA, requisito de pulido production-ready.
Output: tokens ajustados + focus ring en globals.css; focus-trap-react instalado; Drawer y NewAppointmentModal accesibles por teclado y screen reader.

Nota de cobertura: la auditoría de RESEARCH confirma que `STATUS_COLORS` (utils.ts) ya pasa AA, que los avatares de StepDoctor/DoctorsList ya tienen `alt`, y que los icon-buttons de DoctorsList/SpecialtiesList/AvailabilityEditor y el close del Drawer YA tienen `aria-label` (agregados en fases previas). Por eso este plan se concentra en lo que falta: tokens, focus rings y los dos overlays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pulido-y-deploy/05-CONTEXT.md
@.planning/phases/05-pulido-y-deploy/05-RESEARCH.md
@.planning/phases/05-pulido-y-deploy/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar focus-trap-react; ajustar tokens WCAG y agregar focus ring global en globals.css</name>
  <read_first>
    - app/globals.css
    - package.json
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (sección "Contrast Analysis" + "Ajustes de token recomendados")
  </read_first>
  <files>package.json, app/globals.css</files>
  <action>
    Ejecutar `npm install focus-trap-react@12.0.2` (paquete ya aprobado en el checkpoint del plan 05-01; no requiere nuevo checkpoint).
    En `app/globals.css`, dentro del bloque `:root`, cambiar dos tokens que fallan AA como texto (Contrast Analysis): `--success` de `#10b981` a `#047857` (emerald-700, 5.24:1) y `--error` de `#ef4444` a `#b91c1c` (red-700, 6.18:1). NO cambiar `--primary` ni el branding teal (D-07: sólo se ajusta lo que no cumple). NO tocar el bloque `.dark` creado en el plan 05-02.
    Agregar en la sección Base de globals.css una regla global de focus visible: un selector que aplique a elementos interactivos (`a, button, input, select, textarea, [tabindex]`) en estado `:focus-visible` un `outline` o `box-shadow` ring usando el token de foco teal (`--border-focus`), con `outline-offset` pequeño. Quitar el outline por defecto sólo cuando NO es `:focus-visible` (no eliminar outline incondicionalmente — eso rompería accesibilidad por teclado).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/globals.css','utf8'); const p=require('./package.json'); if(!p.dependencies['focus-trap-react']) throw new Error('focus-trap-react no instalado'); if(!/--success:\s*#047857/.test(c)) throw new Error('--success no ajustado'); if(!/--error:\s*#b91c1c/.test(c)) throw new Error('--error no ajustado'); if(!/:focus-visible/.test(c)) throw new Error('falta regla focus-visible'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contiene `focus-trap-react`.
    - `:root` en globals.css define `--success: #047857` y `--error: #b91c1c`.
    - El bloque `.dark` (del plan 05-02) permanece intacto.
    - globals.css contiene una regla `:focus-visible` que aplica un ring teal a `a, button, input, select, textarea, [tabindex]`.
    - No hay regla que elimine `outline` de forma incondicional para esos elementos.
  </acceptance_criteria>
  <done>Tokens WCAG ajustados, focus ring global agregado, focus-trap-react instalado.</done>
</task>

<task type="auto">
  <name>Task 2: Focus trap + Escape + ARIA en Drawer.tsx</name>
  <read_first>
    - components/admin/Drawer.tsx
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (Pattern 5 + Pitfall 3)
  </read_first>
  <files>components/admin/Drawer.tsx</files>
  <action>
    En `components/admin/Drawer.tsx`: importar `FocusTrap from 'focus-trap-react'`. Agregar un `useEffect` (importar `useEffect` de react) que, cuando `open` es true, registre un listener `keydown` global que llame `onClose()` al presionar `Escape`, y lo limpie en el cleanup; dependencias `[open, onClose]`.
    Envolver el `<motion.div>` del PANEL (el segundo, con `className="fixed right-0 top-0 ..."`) en un `<FocusTrap>` con `focusTrapOptions={{ initialFocus: false, allowOutsideClick: true, escapeDeactivates: false }}` (Pattern 5 — el Escape lo maneja el useEffect; `allowOutsideClick` permite cerrar por backdrop).
    Agregar al `<motion.div>` del panel los atributos `role="dialog"`, `aria-modal="true"` y `aria-label={title}` (Pitfall 3). El botón de cierre X ya tiene `aria-label="Cerrar panel"` — no tocarlo. Mantener intactas las animaciones de framer-motion y el backdrop con `onClick={onClose}`.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('components/admin/Drawer.tsx','utf8'); if(!/FocusTrap/.test(c)) throw new Error('falta FocusTrap'); if(!/role=\"dialog\"/.test(c)) throw new Error('falta role dialog'); if(!/aria-modal=\"true\"/.test(c)) throw new Error('falta aria-modal'); if(!/Escape/.test(c)) throw new Error('falta manejo Escape'); if(!/aria-label=\{title\}/.test(c)) throw new Error('falta aria-label title'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `Drawer.tsx` importa `FocusTrap` de `focus-trap-react` y envuelve el panel.
    - El panel tiene `role="dialog"`, `aria-modal="true"`, `aria-label={title}`.
    - Un `useEffect` con dependencia `[open, onClose]` cierra el drawer al presionar `Escape`.
    - `focusTrapOptions` incluye `allowOutsideClick: true` y `escapeDeactivates: false`.
    - El backdrop conserva `onClick={onClose}` y las animaciones framer-motion siguen presentes.
  </acceptance_criteria>
  <done>Drawer accesible: focus atrapado, Escape cierra, ARIA presente.</done>
</task>

<task type="auto">
  <name>Task 3: Focus trap + Escape + ARIA + aria-label close en NewAppointmentModal.tsx</name>
  <read_first>
    - components/admin/NewAppointmentModal.tsx
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (Pattern 5 + Pitfall 3)
  </read_first>
  <files>components/admin/NewAppointmentModal.tsx</files>
  <action>
    En `components/admin/NewAppointmentModal.tsx`: importar `FocusTrap from 'focus-trap-react'`. El modal está siempre montado cuando se renderiza (no usa prop `open` — el padre lo monta/desmonta), así que el focus trap está siempre activo mientras existe. Agregar un `useEffect` (ya se importa `useEffect`) que registre un listener `keydown` global que llame `onClose()` al presionar `Escape`, con cleanup; dependencia `[onClose]`.
    Envolver el panel del modal (el `<div className="relative w-full max-w-lg ...">`) en un `<FocusTrap>` con `focusTrapOptions={{ initialFocus: false, allowOutsideClick: true, escapeDeactivates: false }}`.
    Agregar a ese `<div>` del panel los atributos `role="dialog"`, `aria-modal="true"` y `aria-labelledby` apuntando al id del `<h2>` "Nuevo turno" (agregar `id="new-appointment-title"` al `<h2>` y `aria-labelledby="new-appointment-title"` al panel).
    Agregar `aria-label="Cerrar modal"` al `<button type="button" onClick={onClose}>` que renderiza el ícono `X` (actualmente sin aria-label).
    No alterar la lógica del formulario, los efectos de slots, ni el envío.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('components/admin/NewAppointmentModal.tsx','utf8'); if(!/FocusTrap/.test(c)) throw new Error('falta FocusTrap'); if(!/role=\"dialog\"/.test(c)) throw new Error('falta role dialog'); if(!/aria-modal=\"true\"/.test(c)) throw new Error('falta aria-modal'); if(!/aria-label=\"Cerrar modal\"/.test(c)) throw new Error('falta aria-label close'); if(!/Escape/.test(c)) throw new Error('falta manejo Escape'); if(!/aria-labelledby/.test(c)) throw new Error('falta aria-labelledby'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `NewAppointmentModal.tsx` importa `FocusTrap` y envuelve el panel.
    - El panel tiene `role="dialog"`, `aria-modal="true"` y `aria-labelledby` ligado al `<h2>` con `id="new-appointment-title"`.
    - El botón de cierre X tiene `aria-label="Cerrar modal"`.
    - Un `useEffect` cierra el modal al presionar `Escape`.
    - La lógica del formulario y los efectos de slots permanecen sin cambios.
  </acceptance_criteria>
  <done>NewAppointmentModal accesible: focus atrapado, Escape cierra, ARIA + aria-label presentes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Auditoría WCAG manual (A11Y-01, A11Y-02, A11Y-03)</name>
  <read_first>
    - .planning/phases/05-pulido-y-deploy/05-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>Tokens de contraste ajustados, focus ring global, focus trap + Escape + ARIA en Drawer y NewAppointmentModal. Estas verificaciones requieren browser + teclado + herramienta de contraste, no testeables con unit tests.</what-built>
  <how-to-verify>
    1. A11Y-01 (Escape): en `/admin/especialidades` abrir el Drawer y presionar Escape → cierra. En `/admin/turnos` abrir "Nuevo turno" y presionar Escape → cierra.
    2. A11Y-02 (focus loop): con el Drawer y el modal abiertos, navegar con Tab por todos los campos → el foco cicla dentro del overlay y NO salta al contenido de fondo.
    3. Focus ring: navegar la app con Tab → todos los interactivos muestran un ring teal visible.
    4. A11Y-03 (contraste): con Chrome DevTools → Accessibility/Contrast, verificar texto de éxito (`--success`) y de error (`--error`) sobre fondo claro ≥4.5:1, en light y en dark mode.
    5. Screen reader (opcional): al abrir un overlay se anuncia como diálogo.
  </how-to-verify>
  <resume-signal>Escribir "approved" o describir los problemas de accesibilidad encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` sigue verde.
- `npm run build` compila sin errores de tipos (FocusTrap + ARIA).
- Manual A11Y-01/02/03 según el checkpoint.
</verification>

<success_criteria>
- Tokens de contraste cumplen AA en ambos temas (D-06a, D-07).
- Focus rings visibles en todos los interactivos (D-06b).
- Focus trap + Escape en Drawer y NewAppointmentModal (D-06d).
- aria-label/role/aria-modal en overlays (D-06c).
- (Verificado en research, sin trabajo nuevo) STATUS_COLORS pasa AA; avatares con alt; icon-buttons existentes con aria-label (D-06c, D-06e).
</success_criteria>

<output>
Create `.planning/phases/05-pulido-y-deploy/05-03-SUMMARY.md` when done
</output>
