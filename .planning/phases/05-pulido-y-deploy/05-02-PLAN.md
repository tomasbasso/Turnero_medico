---
phase: 05-pulido-y-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/ThemeToggle.tsx
  - components/admin/AdminSidebar.tsx
  - app/(public)/layout.tsx
  - components/PublicHeader.tsx
  - components/booking/BookingWizard.tsx
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "En la primera visita el tema sigue prefers-color-scheme del sistema (sin elección previa)"
    - "La elección manual del toggle persiste en localStorage y tiene prioridad en visitas siguientes"
    - "No hay flash de tema claro al recargar con preferencia oscura (anti-FOUC)"
    - "El toggle Sun/Moon está visible en el sidebar admin y en el header público"
    - "El panel glassmorphism del wizard /reservar sigue legible en modo oscuro"
    - "Todas las superficies (admin sólido + wizard público) cambian de tema porque los tokens CSS se redefinen bajo .dark"
  artifacts:
    - path: "components/ThemeToggle.tsx"
      provides: "Botón client Sun/Moon que alterna la clase dark y persiste en localStorage"
      contains: "'use client'"
    - path: "components/PublicHeader.tsx"
      provides: "Header público mínimo con logo + ThemeToggle"
    - path: "app/globals.css"
      provides: "@custom-variant dark + bloque .dark con tokens redefinidos"
      contains: "@custom-variant dark"
    - path: "app/layout.tsx"
      provides: "Script anti-FOUC en head + suppressHydrationWarning"
      contains: "suppressHydrationWarning"
  key_links:
    - from: "components/admin/AdminSidebar.tsx"
      to: "components/ThemeToggle.tsx"
      via: "import + render en el footer del sidebar"
      pattern: "ThemeToggle"
    - from: "app/(public)/layout.tsx"
      to: "components/PublicHeader.tsx"
      via: "render del header con ThemeToggle"
      pattern: "PublicHeader"
    - from: "components/ThemeToggle.tsx"
      to: "document.documentElement"
      via: "classList.add/remove('dark') + localStorage 'tm-theme'"
      pattern: "classList\\.(add|remove)\\('dark'\\)"
---

<objective>
Implementar dark mode class-based en toda la app (panel admin + wizard público) sin tocar componentes que ya usan tokens CSS. Cubre D-01..D-05: clase `.dark` en `<html>`, default por `prefers-color-scheme` con persistencia manual en localStorage, anti-FOUC con script inline, y toggle Sun/Moon en sidebar admin + header público.

Purpose: Modo oscuro funcional y sin flash, requisito de pulido production-ready.
Output: `@custom-variant dark` + bloque `.dark` en globals.css, script anti-FOUC en layout.tsx, `components/ThemeToggle.tsx`, `components/PublicHeader.tsx`, toggle montado en sidebar admin y header público, glassmorphism del wizard ajustado para dark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pulido-y-deploy/05-CONTEXT.md
@.planning/phases/05-pulido-y-deploy/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar @custom-variant dark + bloque .dark en globals.css</name>
  <read_first>
    - app/globals.css
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (Pattern 1 + "Code Examples: globals.css bloque completo de dark mode")
  </read_first>
  <files>app/globals.css</files>
  <action>
    En `app/globals.css`, inmediatamente después de `@import "tailwindcss";` (línea 1) y ANTES del bloque `:root`, agregar la directiva `@custom-variant dark (&:where(.dark, .dark *));` — mecanismo oficial de Tailwind v4 para dark mode class-based (Pattern 1).
    Después del bloque `:root` existente (y antes del `@theme inline`), agregar un bloque `.dark { ... }` que redefine los tokens con los valores de RESEARCH.md "Code Examples":
    `--background: #0f172a; --surface: #1e293b; --foreground: #f1f5f9; --primary: #2dd4bf; --primary-hover: #5eead4; --primary-light: #134e4a; --accent: #2dd4bf; --text-primary: #f1f5f9; --text-secondary: #94a3b8; --text-muted: #64748b; --success: #34d399; --warning: #fbbf24; --error: #f87171; --completed: #94a3b8; --border: rgba(148, 163, 184, 0.15); --border-focus: rgba(45, 212, 191, 0.4);` más los `--shadow-sm` y `--shadow-md` dark de RESEARCH.md.
    NO modificar el `@theme inline` (los tokens se mapean a las CSS vars que ya cambian solas). NO tocar los tokens de `:root` en este task — el ajuste WCAG de `--error`/`--success` light pertenece al plan 05-03.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/globals.css','utf8'); if(!/@custom-variant dark/.test(c)) throw new Error('falta @custom-variant'); const idx=c.indexOf('@custom-variant'); const rootIdx=c.indexOf(':root'); if(idx>rootIdx) throw new Error('@custom-variant debe ir antes de :root'); if(!/\.dark\s*\{/.test(c)) throw new Error('falta bloque .dark'); if(!/--background:\s*#0f172a/.test(c)) throw new Error('falta background dark'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contiene `@custom-variant dark (&:where(.dark, .dark *));` posicionado antes del bloque `:root`.
    - Existe un bloque `.dark { }` que define `--background: #0f172a`, `--surface: #1e293b`, `--text-primary: #f1f5f9`, `--primary: #2dd4bf`, `--accent: #2dd4bf` y los demás tokens dark de RESEARCH.md.
    - El bloque `@theme inline` permanece sin cambios.
    - `:root` permanece sin cambios en este task.
  </acceptance_criteria>
  <done>globals.css con variante dark registrada y tokens dark definidos.</done>
</task>

<task type="auto">
  <name>Task 2: Script anti-FOUC + suppressHydrationWarning en layout.tsx</name>
  <read_first>
    - app/layout.tsx
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (Pattern 2 + Pitfall 1 + "Code Examples: Anti-FOUC script")
  </read_first>
  <files>app/layout.tsx</files>
  <action>
    En `app/layout.tsx`, al elemento `<html>` agregar el atributo `suppressHydrationWarning` (sin valor) — necesario porque el script agrega la clase `dark` antes de la hidratación de React (Pitfall 1). Mantener el `className` existente con las variables de fuente intacto.
    Agregar un `<head>` explícito dentro del `<html>` (antes del `<body>`) que contenga un único `<script dangerouslySetInnerHTML={{ __html: '...' }} />` con el script anti-FOUC minificado de RESEARCH.md: lee `localStorage.getItem('tm-theme')`; si es `'dark'` O (no hay valor guardado Y `window.matchMedia('(prefers-color-scheme: dark)').matches`), agrega la clase `dark` a `document.documentElement`. Todo dentro de un `try/catch` vacío. La clave de localStorage DEBE ser exactamente `'tm-theme'` (debe coincidir con la del ThemeToggle del Task 3).
    No usar `<Script>` de next/script — RESEARCH.md (Anti-Patterns) lo descarta para anti-FOUC en App Router; el `<script dangerouslySetInnerHTML>` inline en `<head>` es el patrón correcto.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/layout.tsx','utf8'); if(!/suppressHydrationWarning/.test(c)) throw new Error('falta suppressHydrationWarning'); if(!/dangerouslySetInnerHTML/.test(c)) throw new Error('falta script inline'); if(!/tm-theme/.test(c)) throw new Error('falta clave tm-theme'); if(!/prefers-color-scheme/.test(c)) throw new Error('falta prefers-color-scheme'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `<html>` tiene `suppressHydrationWarning` y conserva su `className` de fuentes.
    - Hay un `<head>` con un `<script dangerouslySetInnerHTML>` que referencia `localStorage`, la clave `'tm-theme'`, `prefers-color-scheme` y `document.documentElement.classList.add('dark')`.
    - El script está envuelto en `try/catch`.
    - No se usa `<Script>` de next/script para el anti-FOUC.
  </acceptance_criteria>
  <done>layout.tsx aplica la clase dark antes del paint sin warnings de hidratación.</done>
</task>

<task type="auto">
  <name>Task 3: Crear ThemeToggle y PublicHeader; montar toggle en sidebar admin y header público; ajustar glassmorphism del wizard</name>
  <read_first>
    - components/admin/AdminSidebar.tsx
    - app/(public)/layout.tsx
    - components/booking/BookingWizard.tsx
    - .planning/phases/05-pulido-y-deploy/05-RESEARCH.md (Pattern 3 + Pitfall 4 + Pitfall 6)
    - lib/utils.ts (para cn)
  </read_first>
  <files>components/ThemeToggle.tsx, components/PublicHeader.tsx, components/admin/AdminSidebar.tsx, app/(public)/layout.tsx, components/booking/BookingWizard.tsx</files>
  <action>
    Crear `components/ThemeToggle.tsx` como Client Component (`'use client'`) siguiendo Pattern 3: estado `dark` inicializado en `false`, sincronizado en `useEffect` con `document.documentElement.classList.contains('dark')` (NO leer localStorage en el render — Pitfall 6). Función `toggle()` que invierte el estado, agrega/quita la clase `dark` en `document.documentElement` y escribe `localStorage.setItem('tm-theme', next ? 'dark' : 'light')`. Renderiza un `<button>` con `aria-label` dinámico (`'Cambiar a modo claro'` / `'Cambiar a modo oscuro'`) que muestra el ícono `Sun` de lucide-react cuando está en dark y `Moon` cuando está en light. Aceptar prop opcional `className`.
    Crear `components/PublicHeader.tsx` (puede ser Server Component que importa el ThemeToggle client): header mínimo con el logo/título "Turnero Médico" a la izquierda y `<ThemeToggle />` a la derecha, usando tokens (`bg-surface`, `border-border`, `text-text-primary`). Header sticky/top con padding moderado.
    En `components/admin/AdminSidebar.tsx`: importar `ThemeToggle` y renderizarlo dentro del bloque footer (el `<div className="border-t border-slate-800 p-3">`), por encima o junto al botón "Cerrar sesión".
    En `app/(public)/layout.tsx`: renderizar `<PublicHeader />` por encima de `{children}` dentro del `<div>` raíz.
    En `components/booking/BookingWizard.tsx`: en el panel glassmorphism (la línea con `bg-white/80 backdrop-blur-md ... border border-accent/20`) agregar la variante `dark:bg-slate-900/80` para que el panel siga legible en dark (Pitfall 4). Revisar las dos orbes de fondo (`bg-accent opacity-10` y `bg-primary opacity-10`) y, si quedan demasiado intensas en dark, reducir a `dark:opacity-5`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('components/ThemeToggle.tsx','utf8'); const h=fs.readFileSync('components/PublicHeader.tsx','utf8'); const s=fs.readFileSync('components/admin/AdminSidebar.tsx','utf8'); const pl=fs.readFileSync('app/(public)/layout.tsx','utf8'); const w=fs.readFileSync('components/booking/BookingWizard.tsx','utf8'); if(!/'use client'/.test(t)) throw new Error('ThemeToggle no es client'); if(!/tm-theme/.test(t)) throw new Error('ThemeToggle sin tm-theme'); if(!/Sun|Moon/.test(t)) throw new Error('ThemeToggle sin iconos'); if(!/aria-label/.test(t)) throw new Error('ThemeToggle sin aria-label'); if(!/ThemeToggle/.test(s)) throw new Error('sidebar sin ThemeToggle'); if(!/PublicHeader/.test(pl)) throw new Error('public layout sin PublicHeader'); if(!/dark:bg-slate-900\/80/.test(w)) throw new Error('wizard sin dark glassmorphism'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/ThemeToggle.tsx` es Client Component, usa la clave `'tm-theme'`, alterna `document.documentElement.classList`, muestra `Sun`/`Moon` y tiene `aria-label` dinámico.
    - `components/PublicHeader.tsx` renderiza el título y `<ThemeToggle />`.
    - `AdminSidebar.tsx` importa y renderiza `<ThemeToggle />` en el footer.
    - `app/(public)/layout.tsx` renderiza `<PublicHeader />` antes de `{children}`.
    - `BookingWizard.tsx` el panel glassmorphism incluye `dark:bg-slate-900/80`.
  </acceptance_criteria>
  <done>Toggle funcional montado en admin y público; wizard legible en dark.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación visual del dark mode (DARK-01, DARK-02)</name>
  <read_first>
    - .planning/phases/05-pulido-y-deploy/05-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>Dark mode class-based completo: variante Tailwind, tokens dark, anti-FOUC, toggle en admin + público, glassmorphism del wizard ajustado. Estas verificaciones son de timing de paint y localStorage, no testeables con unit tests.</what-built>
  <how-to-verify>
    1. Iniciar `npm run dev`. En el OS/DevTools, setear `prefers-color-scheme: dark`. Recargar `/admin` y `/reservar` — NO debe haber flash de tema claro (DARK-01).
    2. Hacer click en el toggle Sun/Moon del sidebar admin → la app cambia de tema. Recargar → el tema elegido se mantiene (DARK-02). Verificar en DevTools → Application → localStorage que existe `tm-theme` con el valor elegido.
    3. Ir a `/reservar` en modo oscuro → el panel del wizard (glassmorphism) y su texto son legibles; las orbes de fondo no saturan.
    4. Confirmar que el toggle también aparece y funciona en el header público de `/reservar`.
  </how-to-verify>
  <resume-signal>Escribir "approved" o describir los problemas visuales encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` sigue verde (no se rompe nada existente).
- Manual DARK-01: sin flash al recargar con preferencia oscura.
- Manual DARK-02: tema persiste en localStorage `tm-theme`.
- Wizard legible en dark.
</verification>

<success_criteria>
- Dark mode class-based funcional en toda la app (D-01, D-03).
- Default por sistema + persistencia manual (D-02).
- Anti-FOUC sin flash (D-04).
- Toggle Sun/Moon en sidebar admin y header público (D-05).
</success_criteria>

<output>
Create `.planning/phases/05-pulido-y-deploy/05-02-SUMMARY.md` when done
</output>
