---
phase: 03-reserva-paciente
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - components/booking/BookingWizard.tsx
  - components/booking/StepProgress.tsx
  - components/booking/StepSpecialty.tsx
  - components/booking/StepDoctor.tsx
autonomous: true
requirements:
  - D-07
must_haves:
  truths:
    - "BookingWizard.tsx gestiona todo el wizard state con useState — 9 campos de estado documentados"
    - "AnimatePresence mode='wait' con key={step} y direction-aware x offset implementado"
    - "StepProgress muestra 5 dots: activo w-3 h-3 bg-accent, completado w-3 h-3 bg-primary/40, próximo w-2 h-2 bg-border"
    - "StepSpecialty hace fetch a /api/public/specialties al montar y muestra 6 skeleton cards durante carga"
    - "Seleccionar especialidad en Step 1 avanza automáticamente a Step 2 (auto-advance per UI-SPEC)"
    - "StepDoctor hace fetch a /api/public/doctors?specialtyId=N y muestra avatar o initials fallback"
    - "Seleccionar médico en Step 2 avanza automáticamente a Step 3"
    - "Steps 3, 4 y 5 tienen placeholders que permiten compilar TypeScript sin errores hasta 03-05"
  artifacts:
    - path: "components/booking/BookingWizard.tsx"
      provides: "Componente raíz 'use client' con todo el wizard state y AnimatePresence"
      exports: ["BookingWizard"]
    - path: "components/booking/StepProgress.tsx"
      provides: "Barra de 5 dots con conectores, aria-label, step label"
      exports: ["StepProgress"]
    - path: "components/booking/StepSpecialty.tsx"
      provides: "Step 1 — grid 2-3 cols, fetch on mount, skeleton, empty state con Stethoscope"
      exports: ["StepSpecialty"]
    - path: "components/booking/StepDoctor.tsx"
      provides: "Step 2 — lista vertical, fetch on mount, avatar/initials, empty state con UserX"
      exports: ["StepDoctor"]
  key_links:
    - from: "components/booking/BookingWizard.tsx"
      to: "components/booking/StepSpecialty.tsx"
      via: "render condicional step === 1, prop onSelect auto-avanza a step 2"
    - from: "components/booking/BookingWizard.tsx"
      to: "components/booking/StepDoctor.tsx"
      via: "render condicional step === 2, prop specialtyId={selectedSpecialty.id}"
    - from: "app/(public)/reservar/page.tsx"
      to: "components/booking/BookingWizard.tsx"
      via: "import nombrado — resuelve el error TypeScript del plan 03-01"
---

<objective>
BookingWizard shell + StepProgress + Steps 1 y 2 (especialidad y médico).

Purpose: La capa UI del wizard comienza aquí. BookingWizard es el único componente con estado — todos los steps son hijos que reciben props y callbacks. Este plan depende de 03-02 porque los steps ya hacen fetch real a las APIs. Steps 3-5 usan placeholders para compilar sin errores hasta que 03-05 los complete.

Output: Cuatro componentes Client bajo components/booking/. El wizard renderiza en /reservar con steps 1 y 2 funcionales. También resuelve el error TypeScript de app/(public)/reservar/page.tsx del plan 03-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reserva-paciente/03-CONTEXT.md
@.planning/phases/03-reserva-paciente/03-UI-SPEC.md
@.planning/phases/03-reserva-paciente/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BookingWizard.tsx y StepProgress.tsx — shell del wizard y barra de progreso</name>
  <read_first>
    - components/admin/Drawer.tsx — leer el archivo completo (líneas 1-52 aprox): patrón AnimatePresence + motion.div con initial/animate/exit. Confirmar: import de AnimatePresence y motion de 'framer-motion', uso de mode="wait" en AnimatePresence.
    - app/(auth)/login/page.tsx — leer líneas 1-60: patrón 'use client' + useState + error state + loading. Y líneas 47-60: glassmorphism card pattern (bg-white/80 backdrop-blur-xl shadow-modal) y gradient blobs teal.
    - .planning/phases/03-reserva-paciente/03-UI-SPEC.md — sección "Wizard Shell": spec de StepProgress (dot sizes w-2/w-3, líneas conectoras h-0.5, label del paso actual, aria-label), y navegación (botón Volver, auto-advance steps 1-2, botones explícitos steps 4-5).
    - app/globals.css — confirmar los design tokens: bg-background, bg-white/80, backdrop-blur-md, shadow-modal, border-accent/20, text-primary, text-accent, bg-accent, bg-primary/40, bg-border.
  </read_first>
  <files>
    components/booking/BookingWizard.tsx,
    components/booking/StepProgress.tsx
  </files>
  <action>
    Crear el directorio components/booking/ y los dos archivos.

    ARCHIVO 1 — components/booking/StepProgress.tsx:
    'use client' como primera línea. Importar cn de '@/lib/utils'. Props: { currentStep: 1|2|3|4|5 }. Definir labels array: ['Especialidad', 'Médico', 'Fecha y hora', 'Tus datos', 'Confirmación'].

    Renderizar un div con className="flex flex-col items-center gap-2 mb-6". Dentro: una fila de dots con conectores (div flex items-center gap-0 w-full max-w-xs mx-auto), y debajo el label del paso actual.

    Lógica de cada uno de los 5 pasos (iterar con índice i de 0 a 4):
    - Si i > 0: línea conectora flex-1 h-0.5. Color: si i < currentStep usa bg-primary/40 (línea completada), sino bg-border.
    - Dot:
        - i+1 === currentStep (activo): className="w-3 h-3 rounded-full bg-accent transition-all"
        - i+1 < currentStep (completado): className="w-3 h-3 rounded-full bg-primary/40 transition-all"
        - i+1 > currentStep (próximo): className="w-2 h-2 rounded-full bg-border transition-all"

    Label debajo: p className="text-sm text-text-secondary text-center" con {labels[currentStep - 1]}.
    Contenedor exterior: div con aria-label={"Paso " + currentStep + " de 5: " + labels[currentStep - 1]}.
    Exportar como función nombrada StepProgress.

    ARCHIVO 2 — components/booking/BookingWizard.tsx:
    'use client' como primera línea. Importar: useState de 'react'; AnimatePresence y motion de 'framer-motion'; StepProgress de './StepProgress'; StepSpecialty de './StepSpecialty'; StepDoctor de './StepDoctor'.

    TIPOS locales (en este mismo archivo):
      type SpecialtyOption = { id: number; name: string; color: string }
      type DoctorOption = { id: number; name: string; bio?: string | null; avatar?: string | null; durationMin: number }

    ESTADO con useState individual (per Claude's Discretion en CONTEXT.md — no useReducer, no Context):
      step: 1|2|3|4|5 — inicia 1
      direction: 1|-1 — inicia 1
      selectedSpecialty: SpecialtyOption | null — inicia null
      selectedDoctor: DoctorOption | null — inicia null
      selectedDate: string | null — inicia null (formato 'YYYY-MM-DD')
      selectedTime: string | null — inicia null (formato 'HH:MM')
      patientName: string — inicia ''
      patientDni: string — inicia ''
      patientPhone: string — inicia ''
      patientInsurance: string — inicia '' (D-05)
      patientEmail: string — inicia ''
      confirmedAppointment: { id: number } | null — inicia null

    FUNCIÓN goTo(nextStep: 1|2|3|4|5):
      setDirection(nextStep > step ? 1 : -1)
      setStep(nextStep)

    FUNCIÓN resetWizard():
      Setear todos los campos a sus valores iniciales (step=1, direction=1, todos null o '').

    RENDER:
    - Div canvas: className="min-h-screen bg-background flex items-start justify-center px-4 py-16"
    - Dentro del canvas: div className="relative w-full max-w-2xl" (para posicionamiento relativo de los blobs)
    - Dos blobs de fondo (aria-hidden, pointer-events-none, fixed):
        blob 1: div className="pointer-events-none fixed inset-0 overflow-hidden" aria-hidden="true" con dos children divs:
          - absolute -top-40 -right-40 h-96 w-96 rounded-full bg-accent opacity-10 blur-3xl
          - absolute -bottom-40 -left-40 h-96 w-96 rounded-full bg-primary opacity-10 blur-3xl
    - Wizard card: div className="relative w-full max-w-2xl bg-white/80 backdrop-blur-md rounded-2xl shadow-modal border border-accent/20 p-6"
    - Dentro de la card: StepProgress currentStep={step}
    - Botón Volver: visible cuando step > 1 && step < 5. Texto "← Volver". className="text-sm text-primary hover:text-accent mb-4 block". onClick: goTo((step - 1) as 1|2|3|4|5).
    - AnimatePresence mode="wait":
        motion.div key={step}:
          initial={{ opacity: 0, x: direction * 40 }}
          animate={{ opacity: 1, x: 0 }}
          exit={{ opacity: 0, x: direction * -40 }}
          transition={{ duration: direction === 1 ? 0.25 : 0.2, ease: 'easeOut' }}
        Dentro del motion.div, render condicional:
          step === 1: StepSpecialty onSelect={(s) => { setSelectedSpecialty(s); goTo(2) }}
          step === 2: selectedSpecialty && StepDoctor con props specialtyId={selectedSpecialty.id}, specialtyName={selectedSpecialty.name}, onSelect={(d) => { setSelectedDoctor(d); goTo(3) }}, onBack={() => goTo(1)}
          step === 3: div con texto "Step 3 — próximamente" (placeholder — se reemplaza en 03-05)
          step === 4: div con texto "Step 4 — próximamente" (placeholder — se reemplaza en 03-05)
          step === 5: div con texto "Step 5 — próximamente" (placeholder — se reemplaza en 03-05)

    El placeholder en steps 3-5 evita errores TypeScript hasta que 03-05 complete.
    Exportar BookingWizard como función nombrada.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/BookingWizard.tsx existe con 'use client', exporta BookingWizard
    - Contiene AnimatePresence mode="wait" con key={step} en el motion.div
    - Contiene los 12 campos de estado (step, direction, selectedSpecialty, selectedDoctor, selectedDate, selectedTime, patientName, patientDni, patientPhone, patientInsurance, patientEmail, confirmedAppointment)
    - Contiene función goTo con setDirection + setStep
    - Contiene función resetWizard
    - components/booking/StepProgress.tsx existe con 'use client', exporta StepProgress
    - StepProgress renderiza dots con tres tamaños distintos (w-2 para próximos, w-3 para activo/completados)
    - StepProgress tiene aria-label con "Paso N de 5: {label}"
    - npx tsc --noEmit pasa sin errores
  </acceptance_criteria>
  <done>BookingWizard y StepProgress implementados. El wizard puede renderizar en /reservar. TypeScript compila (el error de BookingWizard missing del plan 03-01 también se resuelve).</done>
</task>

<task type="auto">
  <name>Task 2: StepSpecialty.tsx y StepDoctor.tsx — pasos 1 y 2</name>
  <read_first>
    - components/admin/SpecialtiesList.tsx — leer líneas 62-95: patrón de card list con colored dot y empty state. Estructura del grid y cómo se usa cn() para clases.
    - app/(auth)/login/page.tsx — leer líneas 1-42: patrón useEffect + fetch + loading + error state. Forma exacta de manejar carga asíncrona en un 'use client' component.
    - lib/utils.ts — leer cn() y getInitials(). cn() para clases condicionales; getInitials() para avatar fallback en Step 2.
    - .planning/phases/03-reserva-paciente/03-UI-SPEC.md — secciones "Step 1: Elegir especialidad" y "Step 2: Elegir médico" — spec completa: grid layout, card anatomy, hover states, loading skeleton, empty state con iconos.
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — secciones "StepSpecialty.tsx" y "StepDoctor.tsx" — patrones de fetch y cn() para cards.
  </read_first>
  <files>
    components/booking/StepSpecialty.tsx,
    components/booking/StepDoctor.tsx
  </files>
  <action>
    ARCHIVO 1 — components/booking/StepSpecialty.tsx:
    'use client' primera línea. Importar: useState, useEffect de 'react'; cn de '@/lib/utils'; Stethoscope de 'lucide-react'.

    Props: { onSelect: (specialty: { id: number; name: string; color: string }) => void }

    Estado: specialties (array vacío), loading: boolean (true), error: string | null (null).

    handleFetch(): fetch('/api/public/specialties').then(r => r.json()).then(data => setSpecialties(data.specialties ?? [])).catch(() => setError('No se pudo cargar las especialidades')).finally(() => setLoading(false)).

    useEffect(() => { handleFetch() }, []).

    RENDER con heading h2 "Elegí una especialidad" className="text-xl font-semibold font-display text-text-primary mb-4":

    Estado LOADING: grid grid-cols-2 gap-3 sm:grid-cols-3, con 6 divs: bg-slate-200 animate-pulse rounded-xl h-24.

    Estado ERROR: div con mensaje error className="text-sm text-error" + button "Intentá nuevamente" que llama setError(null), setLoading(true), handleFetch().

    Estado EMPTY (specialties.length === 0 y !loading y !error):
      div className="flex flex-col items-center justify-center py-16 text-center gap-3"
      Stethoscope className="h-8 w-8 text-text-muted"
      p "No hay especialidades disponibles" className="text-base font-semibold font-display"
      p "El consultorio no tiene especialidades configuradas. Intentá más tarde." className="text-sm text-text-secondary"

    Estado LISTA (specialties.length > 0):
      div grid grid-cols-2 gap-3 sm:grid-cols-3.
      Cada card (div):
        role="button" tabIndex={0}
        onKeyDown: si event.key === 'Enter' o 'Space', prevenir default y llamar onSelect(specialty)
        onClick={() => onSelect(specialty)}
        className={cn(
          'bg-white/70 backdrop-blur-sm rounded-xl shadow-card border p-4 flex flex-col items-center gap-2 cursor-pointer transition-all duration-200',
          'hover:border-accent/40 hover:shadow-modal',
          'border-border'
        )}
        Dentro: div 12×12px rounded-full style={{ backgroundColor: specialty.color }}
        y p className="text-base font-semibold text-text-primary text-center" con {specialty.name}

    Exportar como función nombrada StepSpecialty.

    ARCHIVO 2 — components/booking/StepDoctor.tsx:
    'use client' primera línea. Importar: useState, useEffect de 'react'; cn, getInitials de '@/lib/utils'; UserX, Clock de 'lucide-react'.

    Tipo local: type DoctorOption = { id: number; name: string; bio?: string | null; avatar?: string | null; durationMin: number }

    Props: { specialtyId: number; specialtyName?: string; onSelect: (doctor: DoctorOption) => void; onBack: () => void }

    Estado: doctors: DoctorOption[] (vacío), loading: boolean (true), error: string | null (null).

    useEffect(() => {
      setLoading(true); setError(null)
      fetch('/api/public/doctors?specialtyId=' + specialtyId)
        .then(r => r.json()).then(data => setDoctors(data.doctors ?? []))
        .catch(() => setError('No se pudo cargar los médicos'))
        .finally(() => setLoading(false))
    }, [specialtyId]).

    RENDER con heading h2 "Elegí tu médico" className="text-xl font-semibold font-display text-text-primary mb-1":
    Si specialtyName: p className="text-sm text-text-secondary mb-4" con "Especialidad: {specialtyName}".

    Estado LOADING: 3 divs flex flex-col gap-3, cada uno: bg-slate-200 animate-pulse rounded-xl h-20.

    Estado EMPTY (doctors.length === 0 y !loading y !error):
      flex flex-col items-center justify-center py-12 text-center gap-3
      UserX className="h-8 w-8 text-text-muted"
      p "Sin médicos disponibles" className="text-base font-semibold font-display"
      p "No hay médicos registrados para esta especialidad." className="text-sm text-text-secondary"
      button onClick={onBack} className="mt-2 px-4 py-2 rounded-lg border border-[rgba(13,148,136,0.3)] text-primary text-sm font-semibold transition-all hover:bg-primary/5": "← Volver a especialidades"

    Estado LISTA (doctors.length > 0):
      div className="flex flex-col gap-3".
      Cada card (div):
        role="button" tabIndex={0}
        onKeyDown: Enter/Space → onSelect(doctor)
        onClick={() => onSelect(doctor)}
        className={cn('bg-white/70 backdrop-blur-sm rounded-xl shadow-card border border-border p-4 flex items-start gap-3 cursor-pointer transition-all duration-200 hover:border-accent/40 hover:shadow-modal')}
        Avatar: si doctor.avatar → img src={doctor.avatar} alt={doctor.name} className="w-12 h-12 rounded-full object-cover flex-shrink-0"
                si !doctor.avatar → div className="w-12 h-12 rounded-full bg-primary/20 text-primary text-sm font-semibold flex items-center justify-center flex-shrink-0" con {getInitials(doctor.name)}
        Info div flex flex-col gap-0.5:
          p className="text-base font-semibold text-text-primary" con {doctor.name}
          div className="flex items-center gap-1": Clock className="h-3.5 w-3.5 text-text-secondary" + span className="text-sm text-text-secondary": "{doctor.durationMin} min por turno"
          Si doctor.bio: p className="text-sm text-text-secondary line-clamp-2" con {doctor.bio}

    Exportar como función nombrada StepDoctor.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/StepSpecialty.tsx existe con 'use client', exporta StepSpecialty
    - Contiene handleFetch con fetch('/api/public/specialties') en useEffect
    - Contiene 6 divs skeleton con animate-pulse en loading state
    - Contiene icono Stethoscope en empty state
    - Contiene role="button" y tabIndex={0} en cada specialty card
    - components/booking/StepDoctor.tsx existe con 'use client', exporta StepDoctor
    - Contiene fetch a '/api/public/doctors?specialtyId=' + specialtyId en useEffect([specialtyId])
    - Contiene 3 divs skeleton en loading state
    - Contiene icono UserX en empty state con botón "← Volver a especialidades"
    - Usa getInitials() de '@/lib/utils' para el avatar fallback
    - npx tsc --noEmit pasa sin errores
    - Visita a /reservar en navegador: muestra wizard con step 1 (grilla de especialidades)
  </acceptance_criteria>
  <done>StepSpecialty y StepDoctor implementados con fetch, skeleton, empty state, error state y diseño glassmorphic per UI-SPEC. Steps 1 y 2 son funcionales en /reservar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/public/specialties | Fetch desde Client Component sin datos enviados |
| Browser → GET /api/public/doctors?specialtyId= | Fetch con query param proveniente del state del wizard (no input directo del usuario) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | StepSpecialty/StepDoctor — datos mostrados | accept | Solo se muestran nombre, color (specialty) y nombre, bio, avatar, duración (doctor). Sin PII de pacientes ni credenciales. |
| T-03-04-02 | Tampering | BookingWizard — manipulación de state desde devtools | accept | Estado React en client-side — modificable por devtools pero sin implicancias de seguridad. La validación real ocurre en el servidor en 03-03 (POST /api/public/appointments). |
| T-03-04-03 | Spoofing | Componentes sin autenticación de usuario | accept | El wizard es intencionalmente público — no hay identidad de usuario que proteger. El turno creado requiere datos del paciente que son validados en el servidor. |
| T-03-04-SC | Tampering | npm/npx installs | accept | No se instalan paquetes nuevos — framer-motion, lucide-react, lib/utils ya están en node_modules. |
</threat_model>

<verification>
1. npx tsc --noEmit pasa sin errores en los 4 archivos creados
2. /reservar en el navegador carga el wizard con el step 1 visible (grid de especialidades)
3. Seleccionar una especialidad avanza al step 2 (lista de médicos)
4. Seleccionar un médico avanza al step 3 (placeholder por ahora)
5. El botón "← Volver" aparece en steps 2-4 y navega hacia atrás
</verification>

<success_criteria>
- BookingWizard, StepProgress, StepSpecialty y StepDoctor implementados
- Steps 1 y 2 funcionales en /reservar
- Steps 3-5 tienen placeholders (completan en 03-05)
- TypeScript sin errores
</success_criteria>

<output>
Crear .planning/phases/03-reserva-paciente/03-04-SUMMARY.md al completar.
</output>
