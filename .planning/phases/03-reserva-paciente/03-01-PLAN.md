---
phase: 03-reserva-paciente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - app/(public)/layout.tsx
  - app/(public)/reservar/page.tsx
autonomous: true
requirements:
  - D-05
must_haves:
  truths:
    - "prisma/schema.prisma contiene el campo patientInsurance String? en el modelo Appointment"
    - "npm run db:push aplica la migración sin errores y sin crear carpeta migrations"
    - "GET /reservar devuelve 200 sin sidebar de admin"
    - "app/(public)/layout.tsx NO contiene etiquetas <html> ni <body>"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Campo patientInsurance String? en Appointment, después de patientEmail"
      contains: "patientInsurance"
    - path: "app/(public)/layout.tsx"
      provides: "Layout público sin sidebar — div wrapper min-h-screen bg-background"
    - path: "app/(public)/reservar/page.tsx"
      provides: "Server Component con metadata Reservar turno y render de BookingWizard"
  key_links:
    - from: "app/(public)/reservar/page.tsx"
      to: "components/booking/BookingWizard.tsx"
      via: "import named export BookingWizard (creado en plan 03-04)"
      pattern: "BookingWizard"
---

<objective>
Fundación de la Fase 3: migración de schema (D-05), layout público (public) y ruta /reservar.

Purpose: El campo patientInsurance debe existir en la DB antes de que cualquier API pueda escribirlo (bloquea 03-03). El layout y la ruta shell pueden crearse en Wave 1 sin esperar los otros planes — no tienen lógica propia. Este es el único plan que modifica prisma/schema.prisma.

Output: schema.prisma con patientInsurance, DB migrada vía db:push, layout público sin sidebar, página /reservar como Server Component que importará BookingWizard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reserva-paciente/03-CONTEXT.md
@.planning/phases/03-reserva-paciente/03-RESEARCH.md
@.planning/phases/03-reserva-paciente/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Agregar patientInsurance al schema y ejecutar db:push (D-05)</name>
  <read_first>
    - prisma/schema.prisma — leer el modelo Appointment completo (líneas 60-80 aprox) para conocer la posición exacta donde insertar el campo y confirmar que patientInsurance no existe aún
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — sección "prisma/schema.prisma" — campo exacto a agregar y convención de posición
    - .planning/phases/03-reserva-paciente/03-RESEARCH.md — "Pitfall 3: Prisma migrate vs prisma db push" — confirmar que el proyecto usa db push, NO migrate dev
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
    En el modelo Appointment de prisma/schema.prisma, agregar la línea `patientInsurance  String?` inmediatamente después de la línea `patientEmail    String?`. El campo debe quedar en la posición correcta entre patientEmail y date:

      patientEmail     String?
      patientInsurance String?
      date             DateTime @db.Date

    No mover ni modificar ningún otro campo del modelo. No agregar índices ni constraints para este campo (es solo texto libre opcional, per D-05).

    Después de editar el archivo, ejecutar el push de schema:
      npm run db:push

    Si npm run db:push falla por cualquier razón, usar el fallback:
      npx prisma db push

    IMPORTANTE: NO usar "prisma migrate dev" — el proyecto usa workflow db:push sin carpeta migrations. La columna es nullable (String?) por lo que es una migración aditiva sin pérdida de datos.

    Después del push exitoso, regenerar el Prisma Client:
      npx prisma generate

    Verificar que el comando db:push termine con mensaje que indique sincronización exitosa (exit code 0).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contiene exactamente "patientInsurance  String?" (o "patientInsurance String?") en el modelo Appointment, después de la línea patientEmail
    - npm run db:push (o npx prisma db push) termina con exit code 0
    - No existe carpeta prisma/migrations después de ejecutar el push
    - npx tsc --noEmit pasa sin errores relacionados con tipos Prisma o el schema
  </acceptance_criteria>
  <done>Campo patientInsurance nullable existe en prisma/schema.prisma y en la base de datos Supabase Postgres. TypeScript compila.</done>
</task>

<task type="auto">
  <name>Task 2: Crear layout público y página shell /reservar</name>
  <read_first>
    - app/(admin)/layout.tsx — leer el archivo completo para confirmar que solo envuelve con div (sin html/body), y replicar ese patrón en (public)
    - app/layout.tsx — confirmar que el root layout ya provee html lang="es" y body con fuentes para NO duplicar esas etiquetas en el layout público
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — sección "app/(public)/layout.tsx" — patrón exacto del layout público con advertencia crítica sobre html/body
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — sección "app/(public)/reservar/page.tsx" — patrón de Server Component thin wrapper
  </read_first>
  <files>
    app/(public)/layout.tsx,
    app/(public)/reservar/page.tsx
  </files>
  <action>
    Crear el directorio app/(public)/reservar/ y los archivos necesarios.

    ARCHIVO 1 — app/(public)/layout.tsx:
    Server Component (sin 'use client', sin 'use server'). Exportar la función default PublicLayout que recibe { children: React.ReactNode } y retorna únicamente un div wrapper:
      className="min-h-screen bg-background"
    El div contiene {children}.

    CRÍTICO — NO agregar: etiquetas html, head, body. El root app/layout.tsx ya provee esas etiquetas con las fuentes Outfit+Inter y el lang="es". Si se duplican, Next.js generará errores de hidratación. Verificar este patrón en app/(admin)/layout.tsx — solo envuelve con un div.

    No agregar ningún tipo de navegación, header ni footer en este layout. Es intencionalmente minimalista — el wizard tiene su propio encabezado visual dentro de BookingWizard.tsx.

    ARCHIVO 2 — app/(public)/reservar/page.tsx:
    Server Component (sin 'use client'). Importar Metadata de 'next' y BookingWizard desde '@/components/booking/BookingWizard'. Exportar const metadata: Metadata con title 'Reservar turno — Consultorio Médico' y description 'Reservá tu turno médico en línea, sin registrarte.'. Exportar la función default ReservarPage que retorna simplemente: return <BookingWizard />.

    NOTA: El import de BookingWizard apuntará a un archivo que aún no existe (se crea en plan 03-04). TypeScript reportará error de módulo no encontrado hasta que 03-04 complete — este error es esperado y no impide continuar con otros planes en Wave 1.

    La ruta resultante es /reservar — Next.js App Router ignora los paréntesis en el nombre del route group (public) para la URL final.

    No agregar lógica de fetch de datos, headers() ni cookies() en esta página — es un thin Server Component que solo renderiza el componente client.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "BookingWizard"</automated>
  </verify>
  <acceptance_criteria>
    - app/(public)/layout.tsx existe y la cadena "html" NO aparece como etiqueta JSX (grep: no "<html" en el archivo)
    - app/(public)/layout.tsx contiene "min-h-screen bg-background" en el className del div
    - app/(public)/reservar/page.tsx existe con "export const metadata" y "export default function ReservarPage"
    - app/(public)/reservar/page.tsx importa BookingWizard desde '@/components/booking/BookingWizard'
    - El título en metadata es 'Reservar turno — Consultorio Médico' (texto exacto per UI-SPEC)
    - npx tsc --noEmit sin errores excepto el error esperado de módulo BookingWizard no encontrado
  </acceptance_criteria>
  <done>Layout público y página /reservar existen. La ruta /reservar renderizará BookingWizard cuando el plan 03-04 cree el componente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB (Supabase Postgres) | npx prisma db push escribe directamente con DIRECT_URL en .env |
| Internet → GET /reservar | Ruta pública — cualquier usuario puede acceder sin credenciales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | prisma/schema.prisma — db:push | accept | Columna nullable (String?) — migración aditiva sin riesgo de pérdida de datos; las filas existentes quedan con patientInsurance=NULL. No hay alteración de constraints existentes. |
| T-03-01-02 | Information Disclosure | app/(public)/layout.tsx | accept | Layout no expone datos sensibles; solo estructura HTML sin lógica. |
| T-03-01-03 | Information Disclosure | app/(public)/reservar/page.tsx | accept | No hace fetch de datos en el servidor — es un thin Server Component. Sin PII expuesto en el render. |
| T-03-01-SC | Tampering | npm/npx installs | accept | No se instalan paquetes nuevos en este plan — solo Prisma CLI que ya está en devDependencies (prisma@6.19.3 confirmado en package.json). |
</threat_model>

<verification>
1. npx prisma db push (o npm run db:push) termina con exit 0
2. prisma/schema.prisma contiene "patientInsurance" en el modelo Appointment
3. No existe carpeta prisma/migrations
4. app/(public)/layout.tsx existe sin etiquetas html/body
5. app/(public)/reservar/page.tsx existe con metadata y render de BookingWizard
6. npx tsc --noEmit — solo error esperado de BookingWizard missing; ningún error nuevo
</verification>

<success_criteria>
- Campo patientInsurance existe en DB (db:push exitoso)
- Layout público en su lugar sin romper el root layout
- Ruta /reservar configurada como Server Component shell
- TypeScript no reporta errores en los archivos creados en este plan
</success_criteria>

<output>
Crear .planning/phases/03-reserva-paciente/03-01-SUMMARY.md al completar.
</output>
