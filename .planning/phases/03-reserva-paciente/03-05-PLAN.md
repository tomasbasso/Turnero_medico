---
phase: 03-reserva-paciente
plan: 05
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-04
files_modified:
  - components/booking/StepDateTime.tsx
  - components/booking/StepPatientForm.tsx
  - components/booking/StepConfirmation.tsx
  - components/booking/BookingWizard.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "StepDateTime muestra un calendario mensual con navegación prev/next limitada a la ventana de 30 días (D-06)"
    - "Días disponibles en teal (bg-primary/10), días pasados/fuera de rango en gris cursor-not-allowed (D-07)"
    - "Clic en día disponible hace fetch a /api/public/slots?doctorId=N&date=YYYY-MM-DD y muestra chips (D-08)"
    - "Chips disponibles en teal; chips ocupados en gris opacity-60 cursor-not-allowed; seleccionado con gradient (D-08)"
    - "Cuando el médico no tiene slots en los próximos 30 días, se muestra CalendarX + mensaje + botón volver (D-09)"
    - "El botón 'Confirmar horario' solo se activa cuando hay fecha Y hora seleccionada"
    - "StepPatientForm valida DNI 7-8 dígitos con strip de puntos/espacios en submit (D-01)"
    - "StepPatientForm valida teléfono exactamente 10 dígitos en submit (D-02)"
    - "Campos email y obra social son opcionales (D-03, D-05)"
    - "POST a /api/public/appointments en submit de StepPatientForm; 409 muestra error de race condition inline"
    - "StepConfirmation muestra resumen completo: especialidad, médico, fecha formateada, hora, nombre, DNI, obra social"
    - "Chip de estado PENDIENTE en amber con cuerpo 'Tu turno está pendiente de confirmación por el consultorio.'"
    - "Botón 'Reservar otro turno' en Step 5 llama resetWizard() y vuelve al step 1"
    - "BookingWizard actualizado: reemplaza placeholders de steps 3-5 con StepDateTime, StepPatientForm, StepConfirmation"
  artifacts:
    - path: "components/booking/StepDateTime.tsx"
      provides: "Step 3 — calendario mensual + chips de horarios con fetch por día + stagger animation"
      exports: ["StepDateTime"]
    - path: "components/booking/StepPatientForm.tsx"
      provides: "Step 4 — formulario con validación D-01/D-02/D-03/D-05 + POST /api/public/appointments"
      exports: ["StepPatientForm"]
    - path: "components/booking/StepConfirmation.tsx"
      provides: "Step 5 — pantalla de éxito con resumen del turno, chip PENDIENTE y botón reset"
      exports: ["StepConfirmation"]
    - path: "components/booking/BookingWizard.tsx"
      provides: "Wizard actualizado con steps 3-5 reales (reemplaza placeholders del plan 03-04)"
      exports: ["BookingWizard"]
  key_links:
    - from: "components/booking/StepDateTime.tsx"
      to: "/api/public/slots?doctorId=N&date=YYYY-MM-DD"
      via: "fetch en handleDayClick con doctorId y dateStr"
      pattern: "api/public/slots"
    - from: "components/booking/StepPatientForm.tsx"
      to: "/api/public/appointments"
      via: "POST fetch en handleSubmit con todos los campos del formulario"
      pattern: "api/public/appointments"
    - from: "components/booking/StepConfirmation.tsx"
      to: "lib/utils.ts formatDate + formatTime"
      via: "import nombrado para mostrar fecha en español y hora formateada"
      pattern: "formatDate|formatTime"
---

<objective>
Steps 3, 4 y 5 del wizard + actualización final de BookingWizard — el flujo completo de reserva.

Purpose: Este plan completa el wizard. StepDateTime (calendario + chips) es la pantalla más compleja. StepPatientForm hace el POST real. StepConfirmation muestra el éxito. BookingWizard se actualiza para reemplazar los placeholders con los componentes reales.

Output: Tres nuevos componentes + BookingWizard actualizado. Después de este plan el wizard es completamente funcional end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-reserva-paciente/03-CONTEXT.md
@.planning/phases/03-reserva-paciente/03-UI-SPEC.md
@.planning/phases/03-reserva-paciente/03-PATTERNS.md
@.planning/phases/03-reserva-paciente/03-03-SUMMARY.md
@.planning/phases/03-reserva-paciente/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StepDateTime.tsx — calendario mensual + chips de horarios (D-06, D-07, D-08, D-09)</name>
  <read_first>
    - components/admin/Drawer.tsx — confirmar el patrón AnimatePresence + motion para las animaciones de chips
    - lib/utils.ts — cn() para clases condicionales de celdas del calendario y chips
    - .planning/phases/03-reserva-paciente/03-UI-SPEC.md — sección "Step 3: Elegir fecha y hora" completa: estados de cada celda del calendario (clases Tailwind exactas), navegación prev/next, estados de chips, heading de slots, skeleton de chips, no-availability state, botón "Confirmar horario"
    - .planning/phases/03-reserva-paciente/03-RESEARCH.md — "Pitfall 1: Date Storage vs Display Timezone Mismatch" y el code example completo del calendario en "BookingWizard Shell"
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — sección "StepDateTime.tsx" — cn() para chips, handleDayClick, no-availability message
  </read_first>
  <files>components/booking/StepDateTime.tsx</files>
  <action>
    Crear components/booking/StepDateTime.tsx.

    'use client' primera línea. Importar: useState de 'react'; AnimatePresence y motion de 'framer-motion'; cn de '@/lib/utils'; ChevronLeft, ChevronRight, CalendarX de 'lucide-react'.

    CONSTANTE módulo: const BOOKING_WINDOW_DAYS = 30

    PROPS:
      doctorId: number
      doctorName: string
      selectedDate: string | null         ('YYYY-MM-DD')
      selectedTime: string | null         ('HH:MM')
      onDaySelect: (date: string) => void      — clic en día (padre setea selectedDate, limpia selectedTime)
      onTimeSelect: (time: string) => void     — clic en chip disponible (padre setea selectedTime)
      onConfirm: () => void                    — botón "Confirmar horario" → padre hace goTo(4)
      onBack: () => void                       — botón "← Elegir otro médico"

    ESTADO LOCAL:
      currentMonth: { year: number; month: number } — inicializar con { year: new Date().getFullYear(), month: new Date().getMonth() }
      slots: { time: string; available: boolean }[] — inicia []
      loadingSlots: boolean — inicia false
      noAvailability: boolean — inicia false

    CÓMPUTO del CALENDARIO (inline o en bloque antes del return):
      today = new Date(); today.setHours(0,0,0,0)
      maxDate = new Date(today); maxDate.setDate(today.getDate() + BOOKING_WINDOW_DAYS)
      firstDayOfWeek = new Date(currentMonth.year, currentMonth.month, 1).getDay() — 0=Domingo
      Para el header L-M-X-J-V-S-D (lunes primero), ajustar leadingEmpty:
        Si firstDayOfWeek === 0: leadingEmpty = 6 (domingo = 6 espacios vacíos al inicio para semana Lun-Dom)
        Else: leadingEmpty = firstDayOfWeek - 1
      daysInMonth = new Date(currentMonth.year, currentMonth.month + 1, 0).getDate()

      Helper isDayAvailable(d: number): boolean:
        const cellDate = new Date(currentMonth.year, currentMonth.month, d)  // constructor LOCAL sin Z
        return cellDate >= today && cellDate <= maxDate

      Helper isToday(d: number): boolean:
        const cellDate = new Date(currentMonth.year, currentMonth.month, d)
        return cellDate.toDateString() === today.toDateString()

      Helper dateStr(d: number): string:
        const y = currentMonth.year
        const m = String(currentMonth.month + 1).padStart(2, '0')
        const day = String(d).padStart(2, '0')
        return y + '-' + m + '-' + day

    NAVEGACIÓN MENSUAL:
      canGoPrev: !(currentMonth.year === today.getFullYear() && currentMonth.month === today.getMonth())
      canGoNext: new Date(currentMonth.year, currentMonth.month + 1, 1) <= maxDate
      handlePrevMonth: si canGoPrev, decrementar mes (si month === 0 → year-1, month=11)
      handleNextMonth: si canGoNext, incrementar mes (si month === 11 → year+1, month=0)

    FUNCIÓN handleDayClick(ds: string):
      onDaySelect(ds)  — el padre limpia selectedTime
      setSlots([]); setLoadingSlots(true); setNoAvailability(false)
      fetch('/api/public/slots?doctorId=' + doctorId + '&date=' + ds)
        .then(r => r.json())
        .then(data => {
          const fetchedSlots = data.slots ?? []
          setSlots(fetchedSlots)
          if (fetchedSlots.every((s: { available: boolean }) => !s.available) || fetchedSlots.length === 0) {
            setNoAvailability(true)
          }
        })
        .catch(() => { setSlots([]); setNoAvailability(true) })
        .finally(() => setLoadingSlots(false))

    RENDER:

    Heading h2 "Elegí la fecha y hora" className="text-xl font-semibold font-display text-text-primary mb-1"
    Subtitle p "Dr./Dra. {doctorName}" className="text-sm text-text-secondary mb-4"

    BLOQUE CALENDARIO (div className="bg-white/60 backdrop-blur-sm rounded-xl border border-border p-4 mb-4"):
    Header del mes: div flex justify-between items-center mb-3:
      button onClick={handlePrevMonth} disabled={!canGoPrev} aria-label="Mes anterior": ChevronLeft className="h-5 w-5 text-text-secondary" con disabled className si !canGoPrev
      Heading del mes: p className="text-base font-semibold font-display" con Intl.DateTimeFormat('es-AR', {month:'long', year:'numeric'}).format(new Date(currentMonth.year, currentMonth.month)).replace(/^\w/, c => c.toUpperCase())
      button onClick={handleNextMonth} disabled={!canGoNext}: ChevronRight

    Headers días semana (L M X J V S D): div grid grid-cols-7 gap-1 mb-1:
      ['L','M','X','J','V','S','D'].map(d => div className="text-sm font-semibold uppercase text-text-muted text-center w-10 h-6 flex items-center justify-center")

    Grid de celdas: div grid grid-cols-7 gap-1:
      Array(leadingEmpty).fill(null).map((_, i) => div key={'e'+i} className="w-10 h-10 invisible")
      Array(daysInMonth).fill(null).map((_, i) => {
        const d = i + 1
        const ds = dateStr(d)
        const available = isDayAvailable(d)
        const selected = selectedDate === ds
        const todayMark = isToday(d)
        return div key={d} className={cn(
          'w-10 h-10 flex items-center justify-center rounded-lg text-sm relative',
          selected && 'bg-gradient-to-br from-primary to-accent text-white font-semibold shadow-sm',
          !selected && available && 'bg-primary/10 text-primary font-semibold cursor-pointer hover:bg-primary/20',
          !available && 'text-text-muted cursor-not-allowed'
        )}
        onClick={available ? () => handleDayClick(ds) : undefined}
        role={available ? 'button' : undefined}
        tabIndex={available ? 0 : -1}
        onKeyDown={available ? (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); handleDayClick(ds) } } : undefined}
        aria-label={d + ' de ' + Intl.DateTimeFormat('es-AR', {month:'long'}).format(new Date(currentMonth.year, currentMonth.month)) + (selected ? ', seleccionado' : available ? ', disponible' : ', no disponible')}
        >
          {d}
          {todayMark && <span className="absolute bottom-1 left-1/2 -translate-x-1/2 w-1 h-1 rounded-full bg-accent" />}
        </div>
      })

    BLOQUE TIME SLOTS (div className="mt-4"):
    Heading: p className="text-sm font-semibold uppercase tracking-wide text-text-secondary mb-2": "HORARIOS DISPONIBLES"

    Si !selectedDate: p className="text-sm text-text-muted text-center py-8": "Seleccioná un día para ver los horarios"

    Si selectedDate y loadingSlots:
      div grid grid-cols-4 gap-2 sm:grid-cols-5:
        8 divs: bg-slate-200 animate-pulse rounded-full h-10 w-20

    Si selectedDate y noAvailability y !loadingSlots:
      div className="flex flex-col items-center justify-center py-12 text-center gap-3"
      CalendarX className="h-10 w-10 text-text-muted"
      h3 "Este médico no tiene turnos disponibles" className="text-base font-semibold font-display text-text-primary"
      p "No hay horarios disponibles en los próximos 30 días. Podés elegir otro médico." className="text-sm text-text-secondary"
      button onClick={onBack} className="mt-2 px-4 py-2 rounded-lg border border-[rgba(13,148,136,0.3)] text-primary text-sm font-semibold transition-all hover:bg-primary/5": "← Elegir otro médico"

    Si selectedDate y slots.length > 0 y !loadingSlots:
      AnimatePresence mode="wait":
        div key={selectedDate} className="grid grid-cols-4 gap-2 sm:grid-cols-5":
          slots.map((slot, i) =>
            motion.button key={slot.time}
              initial={{ opacity: 0, y: 8 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.2, ease: 'easeOut', delay: i * 0.03 }}
              disabled={!slot.available}
              onClick={slot.available ? () => onTimeSelect(slot.time) : undefined}
              aria-label={slot.time + (selectedTime === slot.time ? ', seleccionado' : slot.available ? ', disponible' : ', ocupado')}
              className={cn(
                'rounded-full px-3 py-2 text-sm font-semibold min-h-[44px] flex items-center justify-center transition-all duration-150',
                selectedTime === slot.time
                  ? 'bg-gradient-to-r from-primary to-accent text-white shadow-sm'
                  : slot.available
                    ? 'border border-accent/40 text-primary bg-primary/10 cursor-pointer hover:bg-primary/20'
                    : 'bg-slate-100 text-text-muted border border-border cursor-not-allowed opacity-60'
              )}
            >
              {slot.time}
            </motion.button>
          )

    BOTÓN PRIMARIO al final del componente:
      Habilitado solo cuando selectedDate !== null && selectedTime !== null.
      button onClick={selectedDate && selectedTime ? onConfirm : undefined}
      className={cn(
        'w-full rounded-lg py-3 text-sm font-semibold transition-all mt-6',
        selectedDate && selectedTime
          ? 'bg-gradient-to-r from-primary to-accent text-white hover:shadow-[0_0_12px_rgba(20,184,166,0.4)]'
          : 'opacity-50 cursor-not-allowed bg-slate-200 text-text-muted'
      )}
      disabled={!selectedDate || !selectedTime}
      Texto: "Confirmar horario"

    Exportar como función nombrada StepDateTime.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/StepDateTime.tsx existe con 'use client', exporta StepDateTime
    - Contiene BOOKING_WINDOW_DAYS = 30 a nivel módulo
    - Props incluyen onDaySelect, onTimeSelect, onConfirm, onBack
    - Usa new Date(year, month, day) sin Z para construir fechas del calendario
    - handleDayClick llama onDaySelect(ds) y hace fetch a '/api/public/slots?doctorId=...'
    - Chips de slots tienen stagger animation (delay: i * 0.03)
    - noAvailability state muestra CalendarX y botón "← Elegir otro médico"
    - Botón "Confirmar horario" disabled cuando !selectedDate || !selectedTime
    - Accesibilidad: celdas del calendario tienen aria-label descriptivo, role="button" si disponibles
    - npx tsc --noEmit sin errores en este archivo
  </acceptance_criteria>
  <done>StepDateTime completo: calendario mensual con estados visuales per UI-SPEC (D-07), fetch de slots por día (D-08), no-availability state (D-09), stagger animation en chips, botón Confirmar horario activable.</done>
</task>

<task type="auto">
  <name>Task 2: StepPatientForm.tsx + StepConfirmation.tsx + BookingWizard final wiring</name>
  <read_first>
    - app/(auth)/login/page.tsx — leer líneas 90-164: patrón de form con inputs estilizados, error display, loading button con Loader2. Este es el analog exacto para StepPatientForm.
    - lib/utils.ts — leer formatDate y formatTime (las firmas y el locale 'es-AR'). Se usan en StepConfirmation.
    - .planning/phases/03-reserva-paciente/03-UI-SPEC.md — sección "Step 4: Tus datos": tabla de campos, clases de input, mensajes de validación exactos, race condition error. Sección "Step 5: Confirmación": tabla de summary rows, chip PENDIENTE, CTA "Reservar otro turno".
    - .planning/phases/03-reserva-paciente/03-PATTERNS.md — sección "StepPatientForm.tsx": patrón de inputs DNI/phone con inputMode="numeric", validate() function, error por campo, loading button con Loader2. Sección "StepConfirmation.tsx": formatDate/formatTime usage, success animation.
    - components/booking/BookingWizard.tsx (creado en 03-04) — leer el archivo completo para conocer el estado actual antes de modificarlo (reemplazar placeholders de steps 3-5 con los componentes reales).
  </read_first>
  <files>
    components/booking/StepPatientForm.tsx,
    components/booking/StepConfirmation.tsx,
    components/booking/BookingWizard.tsx
  </files>
  <action>
    ARCHIVO 1 — components/booking/StepPatientForm.tsx:
    'use client' primera línea. Importar: useState de 'react'; cn de '@/lib/utils'; Loader2 de 'lucide-react'.

    Props:
      doctorName: string
      selectedDate: string         ('YYYY-MM-DD')
      selectedTime: string         ('HH:MM')
      patientName: string; setPatientName: (v: string) => void
      patientDni: string; setPatientDni: (v: string) => void
      patientPhone: string; setPatientPhone: (v: string) => void
      patientInsurance: string; setPatientInsurance: (v: string) => void
      patientEmail: string; setPatientEmail: (v: string) => void
      doctorId: number
      onSuccess: (appointment: { id: number }) => void   — llamado cuando POST devuelve 201
      onSlotTaken: () => void                             — llamado cuando POST devuelve 409 → volver a step 3

    Estado local: loading: boolean (false), fieldErrors: Record<string, string> ({}).
    Error de slot tomado: error: string | null (null).

    FUNCIÓN validate(): string | null — retorna null si válido, mensaje de error si inválido:
      const dniClean = patientDni.replace(/[\s.]/g, '')
      if (!patientName.trim()) return 'Ingresá tu nombre completo.'
      if (!dniClean) return 'Ingresá tu DNI.'
      if (!/^\d{7,8}$/.test(dniClean)) return 'El DNI debe tener 7 u 8 dígitos numéricos.'
      const phoneClean = patientPhone.replace(/\D/g, '')
      if (!phoneClean) return 'Ingresá tu teléfono.'
      if (phoneClean.length !== 10) return 'El teléfono debe tener exactamente 10 dígitos.'
      if (patientEmail.trim() && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(patientEmail.trim())) return 'Ingresá un email válido.'
      return null

    FUNCIÓN handleSubmit(e: React.FormEvent):
      e.preventDefault()
      const validationError = validate()
      if (validationError) { setError(validationError); return }
      setError(null); setLoading(true)
      const dniClean = patientDni.replace(/[\s.]/g, '')
      const phoneClean = patientPhone.replace(/\D/g, '')
      fetch('/api/public/appointments', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ doctorId, date: selectedDate, time: selectedTime, patientName: patientName.trim(), patientDni: dniClean, patientPhone: phoneClean, patientInsurance: patientInsurance.trim() || null, patientEmail: patientEmail.trim() || null })
      })
        .then(r => r.json().then(data => ({ ok: r.ok, status: r.status, data })))
        .then(({ ok, status, data }) => {
          if (ok) { onSuccess(data.appointment) }
          else if (status === 409) { setError(data.error || 'Este turno ya fue reservado. Elegí otro horario.') }
          else { setError('Ocurrió un error al reservar. Intentá nuevamente.') }
        })
        .catch(() => setError('Ocurrió un error al reservar. Intentá nuevamente.'))
        .finally(() => setLoading(false))

    RENDER — form con onSubmit={handleSubmit}:

    Heading h2 "Tus datos" className="text-xl font-semibold font-display text-text-primary mb-1"
    Subtitle p className="text-sm text-text-secondary mb-4": "Dr./Dra. {doctorName} · {selectedDate} · {selectedTime}" (fechas en raw format — el formateo completo se hace en StepConfirmation)

    div className="flex flex-col gap-4":

    Para cada campo, patrón de label + input (replicar el patrón de login/page.tsx):
      label className="block text-sm text-text-secondary font-medium mb-1" htmlFor={fieldId}
      input con las siguientes especificaciones:
        - Nombre completo: type="text", id="patientName", value={patientName}, onChange: setPatientName, placeholder="Ej: María García", required
        - DNI (D-01): type="text", inputMode="numeric", id="patientDni", value={patientDni}, onChange: e => setPatientDni(e.target.value), placeholder="Ej: 35421789"
        - Teléfono (D-02): type="tel", inputMode="numeric", id="patientPhone", value={patientPhone}, onChange: e => setPatientPhone(e.target.value), placeholder="Ej: 2302587896"
        - Obra social (D-05): type="text", id="patientInsurance", value={patientInsurance}, onChange: e => setPatientInsurance(e.target.value), placeholder="Ej: OSDE, Swiss Medical" (campo opcional)
        - Email (D-03): type="email", id="patientEmail", value={patientEmail}, onChange: e => setPatientEmail(e.target.value), placeholder="Ej: maria@gmail.com" (campo opcional)
      Todos los inputs: className="w-full rounded-lg border border-border bg-white/90 px-3 py-3 min-h-[44px] text-base text-text-primary placeholder:text-text-muted focus:outline-none focus:border-[rgba(13,148,136,0.4)] focus:ring-2 focus:ring-[rgba(13,148,136,0.2)]"
      Nota: text-base (16px) en todos los inputs — previene iOS Safari auto-zoom (Pitfall 5 en RESEARCH.md)

    Error generico / race condition (si error !== null):
      div role="alert" className="text-sm text-error bg-red-50 border border-red-200 rounded-lg px-4 py-3 mt-2"
      Texto: {error}
      Si el error contiene "reservado" (race condition): también mostrar button onClick={onSlotTaken} className="block text-sm text-primary underline mt-1": "← Elegir otro horario"

    Botón submit:
      button type="submit" disabled={loading}
      className="w-full rounded-lg bg-gradient-to-r from-primary to-accent py-3 text-sm font-semibold text-white transition-all hover:shadow-[0_0_12px_rgba(20,184,166,0.4)] hover:opacity-90 disabled:opacity-60 disabled:cursor-not-allowed mt-2"
      Contenido: si loading → span flex items-center justify-center gap-2 con Loader2 h-4 w-4 animate-spin + "Reservando..."
                 si !loading → "Confirmar turno"

    Exportar como función nombrada StepPatientForm.

    ARCHIVO 2 — components/booking/StepConfirmation.tsx:
    'use client' primera línea. Importar: motion de 'framer-motion'; formatDate, formatTime de '@/lib/utils'; CheckCircle2 de 'lucide-react'.

    Props:
      selectedSpecialtyName: string
      selectedDoctorName: string
      selectedDate: string         ('YYYY-MM-DD')
      selectedTime: string         ('HH:MM')
      patientName: string
      patientDni: string
      patientInsurance: string
      onReset: () => void          — llama resetWizard() en BookingWizard

    RENDER:
    div className="flex flex-col items-center text-center py-4":

    motion.div para el icono (entrance animation):
      initial={{ opacity: 0, scale: 0 }}
      animate={{ opacity: 1, scale: 1 }}
      transition={{ duration: 0.4, ease: 'backOut' }}
      className="flex flex-col items-center gap-3 mb-6"
      CheckCircle2 className="h-14 w-14 text-accent"
      h2 "¡Turno reservado!" className="text-3xl font-semibold font-display text-text-primary"

    Confirmation card: div className="bg-primary/10 border border-accent/20 rounded-xl p-5 mt-2 w-full text-left":
      Summary rows — para cada fila: div className="flex justify-between items-baseline py-2 border-b border-border last:border-0"
        span className="text-sm font-semibold uppercase tracking-wide text-text-muted": label
        span className="text-base text-text-primary": value

      Filas en orden:
        { label: 'Especialidad', value: selectedSpecialtyName }
        { label: 'Médico', value: selectedDoctorName }
        { label: 'Fecha', value: formatDate(selectedDate) }
        { label: 'Hora', value: formatTime(selectedTime) }
        { label: 'Paciente', value: patientName }
        { label: 'DNI', value: patientDni }
        { label: 'Obra social', value: patientInsurance || 'No informada' }

    Chip de estado PENDIENTE: div className="mt-4 flex flex-col items-center gap-2":
      span className="bg-amber-100 text-amber-700 rounded-full px-3 py-1 text-sm font-semibold": "PENDIENTE"
      p className="text-sm text-text-secondary text-center": "Tu turno está pendiente de confirmación por el consultorio."
      p className="text-sm text-text-secondary text-center": "Te contactaremos para confirmar el turno."

    Botón reset: button onClick={onReset} className="w-full mt-6 rounded-lg border border-[rgba(13,148,136,0.3)] text-primary py-3 text-sm font-semibold transition-all hover:bg-primary/5": "Reservar otro turno"

    Exportar como función nombrada StepConfirmation.

    ARCHIVO 3 — components/booking/BookingWizard.tsx (ACTUALIZACIÓN):
    Leer el archivo completo primero. Agregar imports: StepDateTime de './StepDateTime', StepPatientForm de './StepPatientForm', StepConfirmation de './StepConfirmation'.

    Agregar handler en BookingWizard:
    - handleDateTimeConfirm: llama goTo(4) (el selectedDate y selectedTime ya están seteados vía onDaySelect/onTimeSelect)
    - handleAppointmentSuccess(appt: { id: number }): setConfirmedAppointment(appt); goTo(5)
    - handleSlotTaken: goTo(3) (vuelve al calendario para elegir otro horario)

    Reemplazar los placeholders de steps 3-5 en el render:

    step === 3:
      StepDateTime con props:
        doctorId={selectedDoctor!.id}
        doctorName={selectedDoctor!.name}
        selectedDate={selectedDate}
        selectedTime={selectedTime}
        onDaySelect={(date) => { setSelectedDate(date); setSelectedTime(null) }}
        onTimeSelect={(time) => setSelectedTime(time)}
        onConfirm={() => goTo(4)}
        onBack={() => goTo(2)}

    step === 4:
      StepPatientForm con props:
        doctorName={selectedDoctor!.name}
        selectedDate={selectedDate!}
        selectedTime={selectedTime!}
        patientName={patientName}; setPatientName={setPatientName}
        patientDni={patientDni}; setPatientDni={setPatientDni}
        patientPhone={patientPhone}; setPatientPhone={setPatientPhone}
        patientInsurance={patientInsurance}; setPatientInsurance={setPatientInsurance}
        patientEmail={patientEmail}; setPatientEmail={setPatientEmail}
        doctorId={selectedDoctor!.id}
        onSuccess={(appt) => { setConfirmedAppointment(appt); goTo(5) }}
        onSlotTaken={() => goTo(3)}

    step === 5:
      StepConfirmation con props:
        selectedSpecialtyName={selectedSpecialty!.name}
        selectedDoctorName={selectedDoctor!.name}
        selectedDate={selectedDate!}
        selectedTime={selectedTime!}
        patientName={patientName}
        patientDni={patientDni}
        patientInsurance={patientInsurance}
        onReset={resetWizard}

    También actualizar el botón "← Volver" en BookingWizard: debe estar oculto en step === 5 (confirmación) — ya está configurado para step < 5 pero verificar que step 5 no lo muestre.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/StepPatientForm.tsx existe con 'use client', exporta StepPatientForm
    - Todos los inputs tienen text-base (no text-sm) — grep confirma que no hay "text-sm" en elementos input
    - Inputs DNI y Teléfono tienen inputMode="numeric"
    - La función validate() valida DNI con regex /^\d{7,8}$/ sobre dniClean con replace de puntos/espacios
    - handleSubmit envía POST a '/api/public/appointments' con Content-Type application/json
    - El error 409 se detecta por status === 409 y muestra el link "← Elegir otro horario"
    - components/booking/StepConfirmation.tsx existe con 'use client', exporta StepConfirmation
    - Muestra formatDate(selectedDate) y formatTime(selectedTime) de lib/utils.ts
    - Incluye chip "PENDIENTE" con className amber
    - Incluye botón "Reservar otro turno" que llama onReset
    - CheckCircle2 tiene animation motion.div con scale 0→1 ease backOut
    - components/booking/BookingWizard.tsx actualizado: steps 3, 4, 5 usan StepDateTime, StepPatientForm, StepConfirmation respectivamente (no más placeholders)
    - npx tsc --noEmit pasa sin errores en todos los archivos modificados
    - npm run build pasa sin errores
  </acceptance_criteria>
  <done>Steps 4 y 5 implementados. BookingWizard completamente cableado. El wizard es funcional end-to-end: especialidad → médico → fecha/hora → datos → confirmación. Turno queda en DB con status PENDING.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patient Browser → GET /api/public/slots?doctorId=&date= | Fetch controlado por state del wizard, no input directo del usuario |
| Patient Browser → POST /api/public/appointments | Datos del formulario del paciente → endpoint público de escritura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | StepPatientForm — manipulación de campos desde devtools antes del POST | accept | El servidor en 03-03 re-valida todos los campos: DNI regex, phone length, email regex, status hardcodeado. La validación client-side es UX, no seguridad. |
| T-03-05-02 | Information Disclosure | StepConfirmation — datos del paciente en pantalla | accept | Información mostrada es la que el mismo paciente ingresó. No se muestran datos de otros pacientes. |
| T-03-05-03 | Denial of Service | POST /api/public/appointments — doble submit | mitigate | Botón disabled durante loading (loading state en StepPatientForm). El @@unique en DB rechaza duplicados exactos con 409. |
| T-03-05-04 | Tampering | StepDateTime — doctorId en fetch de slots | accept | doctorId proviene de selectedDoctor.id (data retornada por la API, no un campo de formulario). El servidor valida que el doctor exista y esté activo. |
| T-03-05-05 | Information Disclosure | StepConfirmation — DNI visible en confirmación | accept | El paciente está viendo su propia información. No hay terceros en el mismo contexto de renderizado. |
| T-03-05-SC | Tampering | npm/npx installs | accept | No se instalan paquetes nuevos — todos los imports son de dependencias existentes (framer-motion, lucide-react, lib/utils). |
</threat_model>

<verification>
1. npx tsc --noEmit pasa sin errores en los 4 archivos de este plan
2. npm run build pasa sin errores
3. Smoke test manual completo del wizard:
   a. GET /reservar → muestra especialidades (Step 1)
   b. Seleccionar especialidad → avanza a Step 2 (médicos)
   c. Seleccionar médico → avanza a Step 3 (calendario)
   d. Clic en día disponible → chips de horarios aparecen con stagger animation
   e. Clic en chip disponible → chip se selecciona en teal/gradient
   f. Clic "Confirmar horario" → avanza a Step 4 (formulario)
   g. Ingresar datos válidos y "Confirmar turno" → POST exitoso → Step 5 (confirmación)
   h. Step 5 muestra resumen completo con chip PENDIENTE
   i. "Reservar otro turno" resetea el wizard a Step 1
4. Race condition test: dos tabs simultáneos con mismo slot → segundo recibe error inline con link "← Elegir otro horario"
5. DNI validation: "12.345.678" → strip de puntos → stored as "12345678"
6. Sin disponibilidad: médico con cero Availability records → mensaje con CalendarX y botón volver
</verification>

<success_criteria>
- Wizard completo y funcional end-to-end desde /reservar
- Turno creado en DB con status PENDING después de un submit exitoso
- Race condition manejada con UI amigable (D-04)
- Validaciones D-01, D-02, D-03, D-05 aplicadas en client-side y server-side
- TypeScript compila sin errores. npm run build exitoso.
</success_criteria>

<output>
Crear .planning/phases/03-reserva-paciente/03-05-SUMMARY.md al completar.
</output>
